# CattitudeFlow 用户手册

## 目录

1. [概述](#概述)
2. [界面构成](#界面构成)
3. [主页](#主页)
4. [笔记界面](#笔记界面)
5. [Ticket界面](#ticket界面)
6. [Focus界面](#focus界面)
7. [设置界面](#设置界面)
8. [搜索语法参考](#搜索语法参考)
9. [快捷键一览](#快捷键一览)
10. [常见问题（FAQ）](#常见问题faq)
11. [故障排除](#故障排除)

---

## 概述

**CattitudeFlow** 是一款将 Cornell 式笔记与 Ticket 管理结合在一起的桌面应用。

笔记正文可使用 Markdown 格式编写，并支持由 Cue（提示）・Note（笔记）・Summary（摘要）3 个面板构成的 Cornell 式格式。  
Ticket 可添加多个 Cornell 式**备忘录**，便于保留与任务相关的详细记录。

---

## 界面构成

### 标题栏导航

应用顶部的标题栏中会显示以下导航按钮。

| 按钮 | 说明 |
|--------|------|
| 主页 | 笔记・Ticket・TODO 的列表与搜索 |
| 笔记 | 创建・编辑 Cornell 式笔记 |
| Ticket | 管理任务・Ticket |
| Focus | 按重要度分数显示笔记/Ticket 列表 |
| 设置 | 应用设置 |

### InfoPill（信息胶囊）

标题栏右侧会显示以下信息。

- **剩余天数**: 试用期剩余天数（例: 剩余 7 天）
- **可用空间**: 磁盘剩余空间状态（OK/少）

### 窗口操作按钮

作为原生应用启动时，显示在标题栏最右侧。

| 按钮 | 操作 |
|--------|------|
| `_` | 最小化 |
| `□` | 最大化/还原 |
| `×` | 关闭 |

---

## 主页

主页采用 3 列布局，由左侧菜单、中间列表、右侧 Peek 预览构成。

### 左侧面板（菜单）

**过滤列表**

| 项目 | 说明 |
|------|------|
| 笔记 | 仅显示笔记 |
| Ticket | 仅显示 Ticket |
| TODO | 显示所有笔记中的 TODO 项 |
| 图片 | 以列表形式显示附件图片 |
| 全部 | 汇总显示 笔记 + Ticket + TODO |

各项目都会显示件数。选择标签时，将以 `标签件数 / 总件数` 的格式显示。

**标签过滤器**

笔记与 Ticket 的标签会合并后以 Chip 形式显示。  
点击标签后，仅显示带有该标签的项目。点击 `全部` 可解除过滤。

### 中间面板（列表）

**搜索栏**

- 占位符: `全文搜索（Ctrl+K） 例: has:url code:powershell foo`
- 使用 `Ctrl+K` 聚焦
- 使用 `清除` 按钮重置搜索词

**过滤 Chip**

搜索栏下方会显示过滤 Chip（全部 / 笔记 / Ticket / TODO）。  
选择 TODO 时，还会显示**范围** Chip（全部 / 今日）。  
启用日期过滤时，会显示 `date: YYYY-MM-DD ×` Chip，点击即可解除。

**列表操作**

| 操作 | 动作 |
|------|------|
| 单击 | 在右侧面板显示 Peek 预览 |
| 双击 | 跳转到对应的笔记/Ticket 界面 |
| `Ctrl+Click` | 跳转到对应界面 |
| `Enter` | 跳转到对应界面 |

**Ticket 的备忘录行**

当 Ticket 中注册了备忘录时，会在 Ticket 行下方以子行形式显示备忘录。

| 操作 | 动作 |
|------|------|
| 单击 | 跳转到 Ticket 界面并打开备忘录 |
| `⛶`（全屏按钮） | 以不显示列表的全屏预览方式显示备忘录 |

**选择图片标签页时**

- 会显示图片缩略图、路径、创建日期、大小、引用来源件数
- 会显示专用搜索栏（按 uid / relPath 搜索）
- 滚动到列表末尾可继续加载更多内容

### 右侧面板（Peek 预览）

单击项目后，右侧面板会显示预览。

- 笔记: Cornell 式预览（Cue / Note / Summary）
- Ticket: Ticket 信息预览
- 图片: 图片预览及其引用来源笔记/Ticket 列表
- Ticket 的备忘录: 备忘录的 Cornell 式预览（无列表面板）
- **全屏按钮** (⛶): 全屏显示预览
- **缩小/放大**: 切换 Cornell 式预览大小（单页显示 / 易读）

### 主页的键盘快捷键

| 快捷键 | 动作 |
|----------------|------|
| `Ctrl+K` | 聚焦到搜索栏 |
| `Ctrl+Shift+T` | 应用今天的日期过滤并显示本月日历 |

---

## 笔记界面

### 界面构成

笔记界面采用**左侧列表**与**右侧编辑器**的 2 列布局。

### 左侧面板（笔记列表）

**新建**

- 通过 `新建` 按钮或 `+` 打开新建笔记对话框
- 在对话框中输入标题和日期（默认: 今天）后创建
- 如果已存在同名标题的笔记，则会打开现有笔记

**创建子笔记**

- 选择笔记时点击 `＋ 子笔记` 按钮，可创建当前所选笔记的子笔记
- 子笔记会在列表中显示为带缩进的行
- 如果当前打开的笔记已经是子笔记，则会创建为与其拥有相同父级的兄弟笔记（不会创建孙级笔记）

**搜索**

- 按 `/` 键聚焦到搜索栏
- 可通过 Host 的 Full-text search 对笔记内容进行全文搜索

**标签过滤器**

点击标签 Chip，可按标签筛选笔记。

**置顶**

- 点击笔记行中的 ★ 图标可切换置顶状态
- 已置顶的笔记显示在列表最上方
- 列表会分为 `已置顶` 与 `全部` 两个区段显示
- 置顶状态会持久保存到 Workspace 中（最多 100 条）

### 右侧面板（编辑器）

**元信息字段**

| 字段 | 说明 |
|------------|------|
| 标题 | 笔记标题（必填） |
| 日期 | YYYY-MM-DD 格式（例: 2026-04-26） |
| 标签 | 逗号分隔（例: ops, daily） |

**Cornell 式编辑器**

笔记正文由以下 3 个面板构成。

| 面板 | 用途 |
|--------|------|
| **Cue**（提示） | 问题・关键词・提示 |
| **Note**（笔记） | 主要内容・详细备注 |
| **Summary**（摘要） | 总结・结论 |

**面板显示模式**

可通过面板模式切换按钮更改显示模式。

| 模式 | 显示 |
|--------|------|
| `1` | 1 面板（仅显示活动面板） |
| `2` | 2 面板 |
| `3` | 3 面板（同时显示所有面板）※默认 |

**标签页切换**

| 标签页 | 说明 |
|------|------|
| 编辑 | CodeMirror 编辑器 |
| 预览 | Markdown 预览（带目录） |

**Markdown 工具栏**

可通过编辑标签页上方的工具栏插入各种格式。

| 按钮 | 格式 |
|--------|------|
| 粗体 | `**文本**` |
| 斜体 | `*文本*` |
| 删除线 | `~~文本~~` |
| 下划线 | `<u>文本</u>` |
| 文字颜色 | `<span style="color:#XXXXXX">文本</span>`（20 种颜色） |
| 项目符号列表 | `- 文本` |
| 编号列表 | `1. 文本` |
| TODO 列表 | `- [ ] 文本` |
| 表格 | 插入表格模板 |
| 提示框 | GFM 提示框（NOTE / TIP / IMPORTANT / WARNING / CAUTION） |
| 标题 | H1 / H2 / H3 |

**文字颜色面板（20 色）**

Red / Crimson / Orange / Amber / Yellow / Lime / Green / Teal / Cyan / Sky /  
Blue / Indigo / Purple / Violet / Pink / Rose / Brown / Slate / Gray / Black

**粘贴图片**

将剪贴板中的图片粘贴到编辑器时，会自动保存到 Workspace，并以 Markdown 图片语法插入。  
导入过程中会显示“正在导入图片…”的覆盖层。

**保存状态**

右上角会显示保存状态。

| 状态 | 显示 |
|------|------|
| 已保存 | `已保存` |
| 未保存 | `未保存` |
| 保存中 | `保存中…` |

**删除**

可通过垃圾桶图标删除笔记。若笔记处于置顶状态或存在未保存内容，则会显示警告。  
如果存在子笔记，将显示确认对话框，询问是否连同子笔记一起删除。

### 笔记界面的键盘快捷键

| 快捷键 | 动作 |
|----------------|------|
| `Ctrl+Enter` 或 `Ctrl+S` | 保存 |
| `Ctrl+1` | 跳转到 Cue 面板 |
| `Ctrl+2` | 跳转到 Note 面板 |
| `Ctrl+3` | 跳转到 Summary 面板 |
| `Ctrl+E` | 切换到编辑标签页 |
| `Ctrl+P` | 切换到预览标签页 |
| `/` | 聚焦到笔记搜索栏 |

---

## Ticket界面

Ticket 是用于管理任务和 ToDo 的功能。支持 Cornell 式正文、状态管理、评论与备忘录。

### 左侧面板（Ticket 列表）

**搜索・过滤**

| 操作 | 说明 |
|------|------|
| `/` 键 | 聚焦到搜索栏 |
| 优先级过滤 | 全部 / P0 / P1 / P2 / P3 |
| 排序 | 更新日期 / 截止日期 / 优先级 |

**新建**

- 点击 `新建` 按钮后会显示标题输入表单
- 输入标题后按 `Enter` 创建

**备忘录列表**

选择 Ticket 后，该 Ticket 中注册的备忘录会在下方以列表形式显示。

| 操作 | 说明 |
|------|------|
| 点击备忘录行 | 在右侧面板中打开该备忘录 |
| `👁` 图标 | 在列表中隐藏/重新显示备忘录 |
| `🗑` 图标 | 删除备忘录（带确认对话框） |

### 右侧面板（Ticket 编辑器）

选择 Ticket 后，右侧面板会显示编辑器。

**元信息字段**

| 字段 | 说明 |
|------------|------|
| 标题 | Ticket 标题（必填） |
| 状态 | 自定义列（可在设置界面修改。例: backlog / doing / done） |
| 截止日期 | YYYY-MM-DD 格式 |
| 优先级 | P0（最高）/ P1 / P2 / P3（最低） |
| 标签 | 逗号分隔（例: urgent, ops） |

**标签页切换**

| 标签页 | 说明 |
|------|------|
| 编辑 | Cornell 式编辑器（Cue / Note / Summary） |
| 预览 | Markdown 预览（Cornell 式 3 面板） |
| 评论（n） | 添加・查看评论 |

**删除**

使用垃圾桶图标删除 Ticket（带确认对话框）。  
若已注册备忘录，会显示包含备忘录数量的确认消息。

### 备忘录功能

一个 Ticket 可添加多个 Cornell 式备忘录。它有助于记录会议纪要、调查笔记、参考资料等与 Ticket 相关的各种信息。

**添加备忘录**

点击左侧面板底部的 `＋ 添加备忘录` 按钮后，会立即创建备忘录，并在右侧面板显示备忘录编辑器。

**备忘录编辑器**

备忘录编辑器与 Ticket 正文一样，由 Cornell 式 3 面板（Cue / Note / Summary）构成。

| 标签页 | 说明 |
|------|------|
| 编辑 | Cornell 式编辑器（Cue / Note / Summary） |
| 预览 | Markdown 预览（Cornell 式 3 面板） |

- 输入标题后会开始自动保存
- 修改内容后会在 800ms 后自动保存

**备忘录操作**

| 操作 | 说明 |
|------|------|
| 点击备忘录行 | 打开备忘录编辑器 |
| `👁` 图标 | 在列表中隐藏/重新显示备忘录（数据会保留） |
| `🗑` 图标 | 删除备忘录（带确认对话框） |
| `⛶`（全屏） | 全屏显示备忘录（可从主页操作） |

> 备忘录与 Ticket 绑定，删除 Ticket 时，备忘录也会一并删除。

### Ticket界面的键盘快捷键

| 快捷键 | 动作 |
|----------------|------|
| `Ctrl+Enter` | 保存 |
| `/` | 聚焦到搜索栏 |

---

## Focus界面

这是一个按分数顺序显示重要笔记或 Ticket，用于集中进行审阅与查看的界面。

### 显示模式

| 模式 | 说明 |
|--------|------|
| 笔记 | 按分数顺序显示笔记 |
| Ticket | 按分数顺序显示 Ticket |

### 评分

笔记分数由以下要素计算。

- **编辑次数**（过去 30 天）: 可在设置中切换 ON/OFF
- **被引用次数（incomingLinks）**: 被其他笔记引用的次数

### 界面构成

| 区域 | 内容 |
|------|------|
| 左侧列表 | 排名・标题・引用次数・更新日期 |
| 右侧预览 | 所选笔记的 Cornell 式预览 |
| `最大` | 列表中显示的最大条数（5〜200） |

### 操作

| 操作 | 动作 |
|------|------|
| 点击 | 选择笔记并显示预览 |
| 双击 / `Enter` | 在笔记/Ticket 界面中打开 |
| `全屏查看` 按钮 | 打开全屏查看器 |

### 全屏查看器

- 可在左侧列表中切换项目
- 可通过搜索栏搜索标题
- 使用 `打开` 按钮跳转到对应界面
- 使用 `ESC` 或 `关闭` 结束

### Focus界面的键盘快捷键

| 快捷键 | 动作 |
|----------------|------|
| `Enter` | 打开当前选中的项目 |
| `ESC` | 关闭全屏查看器 |

---

## 设置界面

可通过标题栏中的 **设置** 修改各项设置。在左侧边栏中选择类别。

### 常规（General）

| 设置 | 说明 |
|------|------|
| 语言 | UI 显示语言（system / ja / en / zh / ko / fr / de） |
| 区域设置 | 数值・日期格式的 locale（留空时使用 OS 设置，例: ja-JP） |
| 主题预设 | 配色主题 |

- `预览应用`: 立即预览所选主题
- `保存主题`: 将主题保存到 Host
- `保存语言/区域设置`: 保存语言设置

### Workspace

| 设置 | 说明 |
|------|------|
| WorkspaceRoot | 笔记与 Ticket 的保存路径 |

- `重新加载`: 重新获取当前 Workspace 设置
- `保存`: 保存修改后的路径

### 笔记

| 设置 | 说明 |
|------|------|
| 自动保存 | 输入时是否自动保存（ON/OFF） |
| 自动保存间隔（ms） | 自动保存间隔（200〜10,000ms，UI 本地设置） |

### Ticket

| 设置 | 说明 |
|------|------|
| 自动保存 | 编辑 Ticket 时是否自动保存（ON/OFF） |
| 自动保存间隔（ms） | 自动保存间隔（UI 本地设置） |
| 列（Columns） | Ticket 的状态候选项（逗号分隔）<br>例: `backlog, doing, done, blocked, archived` |
| 显示 Ticket ID | 在列表/预览等位置显示 Ticket ID（UI 本地设置） |

### Focus

| 设置 | 说明 |
|------|------|
| 最大条数 | Focus 界面列表的最大条数 |
| 将编辑次数用于排名 | 将 note.save 的次数计入 Focus 分数 |
| 活动日志（Activity） | 启用编辑次数统计（不会保存正文・搜索查询） |

> ※ 由 autoSave 触发的连续保存会按“每分钟 1 次”取整计数。

### 维护

| 操作 | 说明 |
|------|------|
| **导出 Zip** | 将 Workspace 备份为 Zip 文件 |
| **从 Zip 恢复** | 从 Zip 恢复笔记・Ticket（现有数据会被替换） |
| **打开日志文件夹** | 在文件管理器中打开应用日志文件夹 |
| **导出日志 Zip** | 以 Zip 格式导出日志 |
| **打开许可证文件夹** | 打开随附许可证文件所在文件夹 |

> ⚠️ **恢复操作会完全替换现有的笔记和 Ticket。** 请务必先做好备份后再执行。

---

## 搜索语法参考

在主页的全文搜索栏中，可使用以下特殊语法。

| 语法 | 说明 | 例子 |
|------|------|----|
| 普通关键词 | 以空格分隔进行 AND 搜索 | `备忘录 会议` |
| `"短语"` | 短语搜索（将多个词作为一个整体搜索） | `"今天的工作"` |
| `has:url` | 搜索包含 URL 的笔记/Ticket | `has:url` |
| `code:any` | 搜索包含代码块的项目 | `code:any` |
| `code:<语言>` | 搜索包含指定语言代码块的项目 | `code:powershell` |

**例:**
```
has:url code:powershell 备忘录
```
→ 包含 URL、包含 PowerShell 代码块，并包含“备忘录”这一关键词的项目

---

## 快捷键一览

### 全局

| 快捷键 | 动作 |
|----------------|------|
| `Ctrl+K` | 聚焦到主页搜索栏 |
| `Ctrl+Shift+T` | 应用今天的日期过滤 |
| `Ctrl+鼠标滚轮` | 放大/缩小界面 |
| `Ctrl+0` | 将缩放重置为 100% |

### 笔记界面

| 快捷键 | 动作 |
|----------------|------|
| `Ctrl+Enter` / `Ctrl+S` | 保存 |
| `Ctrl+1` | 跳转到 Cue 面板 |
| `Ctrl+2` | 跳转到 Note 面板 |
| `Ctrl+3` | 跳转到 Summary 面板 |
| `Ctrl+E` | 切换到编辑标签页 |
| `Ctrl+P` | 切换到预览标签页 |
| `/` | 聚焦到搜索栏 |

### Ticket界面

| 快捷键 | 动作 |
|----------------|------|
| `Ctrl+Enter` | 保存 |
| `/` | 聚焦到搜索栏 |

### Focus界面

| 快捷键 | 动作 |
|----------------|------|
| `Enter` | 打开当前选中的项目 |
| `ESC` | 关闭全屏查看器 |

### 主页（列表操作）

| 快捷键 | 动作 |
|----------------|------|
| `Enter` | 跳转到当前选中的项目 |
| `Ctrl+Click` | 跳转到当前选中的项目 |

---

## 常见问题（FAQ）

### Q. Focus 的分数是如何决定的？
**A.** 由以下要素计算。
- **编辑次数（edit30d）**: 过去 30 天的保存次数（连续自动保存会按每分钟 1 次取整）
- **被引用次数（incomingLinks）**: 被其他笔记链接的次数

可在 设置 → Focus → **将编辑次数用于排名** 中切换 ON/OFF。

---

### Q. 可以给 Ticket 添加任意数量的备忘录吗？
**A.** 数量没有限制。可通过 `＋ 添加备忘录` 按钮添加。不再需要的备忘录可通过垃圾桶图标删除，如果只是想暂时隐藏，则可使用 👁 图标隐藏。

---

### Q. 如何删除 TODO？
**A.** TODO 是笔记中的 `- [ ] 文本` 格式行。编辑笔记时删除对应行，或将其标记为已完成，如 `- [x] 文本`。主页中的 TODO 列表会自动更新。

---

### Q. 我想新增或修改 Ticket 的状态
**A.** 请在 设置 → Ticket → **列（Columns）** 中，以逗号分隔输入状态名称并保存。  
例: `backlog, doing, done, blocked, archived`

---

### Q. 可以在多台设备之间同步吗？
**A.** 通过将共享文件夹（NAS 或云同步文件夹）指定为 WorkspaceRoot，可以进行访问，但**由于同时编辑可能导致数据冲突，因此不推荐**。

---

### Q. 子笔记还能继续拥有子笔记吗？
**A.** 子笔记的嵌套限制为 1 层。在选中子笔记时按下 `＋ 子笔记`，会将其创建为与该父级相同父级的兄弟笔记（不会创建孙级笔记）。

---

### Q. 自动保存和置顶可以同时使用吗？
**A.** 可以。自动保存会定期保存笔记正文与元信息，而置顶状态会通过另一套机制保存。

---

## 故障排除

### 应用无法启动
1. 确认 **WebView2 Runtime** 是否已安装
2. 确认 Host 端（后端进程）是否正常启动
3. 通过 设置 → 维护 → **打开日志文件夹** 查看错误日志

### 无法读取数据 / 显示错误

| 错误 | 处理方法 |
|--------|------|
| `E_WS_NOT_WRITABLE` | 确认 Workspace 文件夹的写入权限 |
| `E_WS_INVALID_ROOT` | 在 设置 → Workspace 中确认并修正路径 |
| `E_APP_NOT_READY` | 等待启动完成后再重试 |
| `E_WEBVIEW2_UNAVAILABLE` | 安装 WebView2 Runtime |
| `E_SETTINGS_CORRUPT` | 设置将被自动初始化。请重新进行设置 |

### 自动保存不起作用
1. 确认 设置 → 笔记/Ticket → **自动保存** 是否已启用
2. 确认 autoSaveMs 是否在 `200〜10,000` 范围内
3. 确认笔记/Ticket 内容是否确实发生了变更（无变更时不会保存）

### 搜索结果不显示
1. 检查搜索词拼写
2. 检查是否启用了标签过滤或日期过滤（点击过滤 Chip 上的 `×` 可解除）
3. 将过滤器切换为 **全部** 后重新搜索

### Peek 预览不显示
- 点击项目时请使用**单击**，而不是双击
- 如果窗口宽度过窄，请加宽窗口以显示右侧面板

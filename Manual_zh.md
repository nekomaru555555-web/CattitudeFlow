# CattitudeFlow 用户手册

## 目录

1. [概述](#概述)
2. [界面构成](#界面构成)
3. [主页](#主页)
4. [笔记界面](#笔记界面)
5. [工单界面](#工单界面)
6. [专注界面](#专注界面)
7. [OCR备忘录界面](#ocr备忘录界面)
8. [设置界面](#设置界面)
9. [搜索语法参考](#搜索语法参考)
10. [快捷键一览](#快捷键一览)
11. [常见问题（FAQ）](#常见问题faq)
12. [故障排除](#故障排除)

---

## 概述

**CattitudeFlow** 是一款将康奈尔式笔记与工单管理结合在一起的桌面应用。

笔记正文可使用 Markdown 格式编写，并支持由 Cue（提示）・Note（笔记）・Summary（摘要）3 个面板构成的康奈尔式格式。  
工单可添加多个康奈尔式**备忘录**，便于保留与任务相关的详细记录。  
v1.0.5 在可将屏幕文字直接导入应用的 **OCR 捕获** 功能之外，还新增了可在正文上自由放置的 **便签**、面向屏幕共享的 **演示视图**、以及 **Markdown 数学公式（KaTeX）** 等功能。

---

## 界面构成

### 标题栏导航

应用顶部的标题栏中会显示以下导航按钮。

| 按钮 | 说明 |
|--------|------|
| 主页 | 笔记・工单・TODO 的列表与搜索 |
| 笔记 | 创建・编辑康奈尔式笔记 |
| 工单 | 管理任务・工单 |
| 专注 | 按重要度分数显示笔记/工单列表 |
| OCR备忘录 | 查看并编辑通过 OCR 捕获导入的备忘录 |
| 设置 | 应用设置 |

### 信息胶囊（InfoPill）

标题栏右侧会显示以下信息。

- **剩余天数**: 试用期剩余天数（例: 剩余 7 天）
- **可用空间**: 磁盘剩余空间状态（OK/少）

### 窗口操作按钮

作为原生应用启动时，显示在标题栏最右侧。

| 按钮 | 操作 |
|--------|------|
| `_` | 最小化 |
| `□` | 最大化/还原 |
| `×` | 关闭 |

### 标题栏快捷按钮

| 按钮 | 操作 |
|--------|------|
| `✂` | 打开代码片段管理对话框（`Ctrl+I`） |
| `☀` | 开启/关闭聚光灯光标 |
| `☀ ▾` | 切换当前聚光灯预设 |
| `🖥` 演示视图 | 开关 **演示视图**，提升实时预览/预览的可读性（面向屏幕共享） |

代码片段按钮会显示已保存数量徽标。聚光灯图标的颜色会跟随当前选中的预设。

**演示视图** 会强调实时预览和预览的字号、行距与对比度，使其在屏幕共享时也清晰易读。开关状态会记忆在应用内，不影响主题配色或已保存的数据。

### 系统托盘菜单

右键单击系统托盘图标后，会显示以下菜单。

| 项目 | 说明 |
|------|------|
| 显示 / 隐藏 | 切换主窗口的显示与隐藏 |
| 聚光灯 | 开启/关闭聚光灯光标 |
| 代码片段 | 打开代码片段管理对话框 |
| OCR | 开始屏幕捕获 |
| 退出 | 关闭应用 |

各项目右侧会显示当前设置的全局热键。  
热键可在 设置 → 快捷键 中自定义。

---

## 主页

主页采用 3 列布局，由左侧菜单、中间列表、右侧 Peek 预览构成。

### 左侧面板（菜单）

**过滤列表**

| 项目 | 说明 |
|------|------|
| 笔记 | 仅显示笔记 |
| 工单 | 仅显示工单 |
| TODO | 显示所有笔记中的 TODO 项 |
| 图片 | 以列表形式显示附件图片 |
| 全部 | 汇总显示 笔记 + 工单 + TODO |

各项目都会显示件数。选择标签时，将以 `标签件数 / 总件数` 的格式显示。

**标签过滤器**

笔记与工单的标签会合并后以 Chip 形式显示。  
点击标签后，仅显示带有该标签的项目。点击 `全部` 可解除过滤。

### 中间面板（列表）

**搜索栏**

- 占位符: `全文搜索（Ctrl+K） 例: has:url code:powershell foo`
- 使用 `Ctrl+K` 聚焦
- 使用 `清除` 按钮重置搜索词
- 快速输入 `::` 可打开已固定代码片段的模态窗口

**过滤 Chip**

搜索栏下方会显示过滤 Chip（全部 / 笔记 / 工单 / TODO）。  
选择 TODO 时，还会显示**范围** Chip（全部 / 今日）。  
启用日期过滤时，会显示 `date: YYYY-MM-DD ×` Chip，点击即可解除。

**列表操作**

| 操作 | 动作 |
|------|------|
| 单击 | 在右侧面板显示 Peek 预览 |
| 双击 | 跳转到对应的笔记/工单界面 |
| `Ctrl+Click` | 跳转到对应界面 |
| `Enter` | 跳转到对应界面 |

**工单的备忘录行**

当工单中注册了备忘录时，会在工单行下方以子行形式显示备忘录。  
带有备忘录的工单会显示 `▼ / ▶` 切换按钮，可展开或折叠备忘录树。

| 操作 | 动作 |
|------|------|
| 单击 | 跳转到工单界面并打开备忘录 |
| `⛶`（全屏按钮） | 以不显示列表的全屏预览方式显示备忘录 |

**选择图片标签页时**

- 会显示图片缩略图、路径、创建日期、大小、引用来源件数
- 会显示专用搜索栏（按 uid / relPath 搜索）
- 滚动到列表末尾可继续加载更多内容

### 右侧面板（Peek 预览）

单击项目后，右侧面板会显示预览。

- 笔记: 康奈尔式预览（Cue / Note / Summary）
- 工单: 工单信息预览
- 图片: 图片预览及其引用来源笔记/工单列表
- 工单的备忘录: 备忘录的康奈尔式预览（无列表面板）
- **全屏按钮** (⛶): 全屏显示预览
- **缩小/放大**: 切换康奈尔式预览大小（单页显示 / 易读）
- 在外部编辑器中修改文件后，列表与预览也会自动刷新

### 主页的键盘快捷键

| 快捷键 | 动作 |
|----------------|------|
| `Ctrl+K` | 聚焦到搜索栏 |
| `Ctrl+Shift+T` | 应用今天的日期过滤并显示本月日历 |

---

## 笔记界面

### 界面构成

笔记界面采用**左侧列表**与**右侧编辑器**的 2 列布局。

### 左侧面板（笔记列表）

**新建**

- 通过 `新建` 按钮或 `+` 打开新建笔记对话框
- 在对话框中输入标题和日期（默认: 今天）后创建
- 如果已存在同名标题的笔记，则会打开现有笔记

**创建子笔记**

- 选择笔记时点击 `＋ 子笔记` 按钮，可创建当前所选笔记的子笔记
- 子笔记会在列表中显示为带缩进的行
- 如果当前打开的笔记已经是子笔记，则会创建为与其拥有相同父级的兄弟笔记（不会创建孙级笔记）

**搜索**

- 按 `/` 键聚焦到搜索栏
- 可通过 Host 的 Full-text search 对笔记内容进行全文搜索

**标签过滤器**

点击标签 Chip，可按标签筛选笔记。

**置顶**

- 点击笔记行中的 ★ 图标可切换置顶状态
- 已置顶的笔记显示在列表最上方
- 列表会分为 `已置顶` 与 `全部` 两个区段显示
- 置顶状态会持久保存到 Workspace 中（最多 100 条）

### 右侧面板（编辑器）

**元信息字段**

| 字段 | 说明 |
|------------|------|
| 标题 | 笔记标题（必填） |
| 日期 | YYYY-MM-DD 格式（例: 2026-04-26） |
| 标签 | 逗号分隔（例: ops, daily） |

**康奈尔式编辑器**

笔记正文由以下 3 个面板构成。

| 面板 | 用途 |
|--------|------|
| **Cue**（提示） | 问题・关键词・提示 |
| **Note**（笔记） | 主要内容・详细备注 |
| **Summary**（摘要） | 总结・结论 |

**面板显示模式**

可通过面板模式切换按钮更改显示模式。

| 模式 | 显示 |
|--------|------|
| `1` | 1 面板（仅显示活动面板） |
| `2` | 2 面板 |
| `3` | 3 面板（同时显示所有面板）※默认 |

切换后的模式会立即生效，并在下次重新打开笔记时恢复此前的显示模式。

**标签页切换**

| 标签页 | 说明 |
|------|------|
| 编辑 | CodeMirror 编辑器 |
| 预览 | Markdown 预览（带目录） |

- 编辑时按 `Ctrl+F` 可打开与应用显示语言一致的搜索面板
- 预览标签页支持导出 PDF，并保留当前背景色

**Markdown 工具栏**

可通过编辑标签页上方的工具栏插入各种格式。

| 按钮 | 格式 |
|--------|------|
| 粗体 | `**文本**` |
| 斜体 | `*文本*` |
| 删除线 | `~~文本~~` |
| 下划线 | `<u>文本</u>` |
| 文字颜色 | `<span style="color:#XXXXXX">文本</span>`（20 种颜色） |
| 项目符号列表 | `- 文本` |
| 编号列表 | `1. 文本` |
| TODO 列表 | `- [ ] 文本` |
| 表格 | 插入表格模板 |
| 提示框 | GFM 提示框（NOTE / TIP / IMPORTANT / WARNING / CAUTION） |
| 标题 | H1 / H2 / H3 |

**文字颜色面板（20 色）**

Red / Crimson / Orange / Amber / Yellow / Lime / Green / Teal / Cyan / Sky /  
Blue / Indigo / Purple / Violet / Pink / Rose / Brown / Slate / Gray / Black

**粘贴图片**

将剪贴板中的图片粘贴到编辑器时，会自动保存到 Workspace，并以 Markdown 图片语法插入。  
导入过程中会显示“正在导入图片…”的覆盖层。

**便签**

可在笔记正文上方放置可自由摆放的便签。在编辑器正文上右键单击，从上下文菜单中选择 `添加便签`。

- 新添加的便签会自动获得焦点，可立即开始输入（输入过程中不会被抢走焦点）
- 拖动顶部的条移动便签。便签锚定在正文中的位置，会随滚动和编辑一起移动
- 用左上角的颜色按钮切换颜色（黄 → 粉 → 蓝 → 绿 循环）
- 拖动文本区域右下角可调整大小，调整后的大小会被保存
- 点击 `✕` 按钮删除（带确认对话框）
- 按 `Ctrl+;` 插入日期，按 `Ctrl+Shift+;` 插入时间
- 便签保存在笔记正文中，因此也会包含在 Zip 备份内

**数学公式（Math）**

Markdown 预览和实时预览支持通过 KaTeX 渲染数学公式。

- 行内公式: `$E = mc^2$`
- 块级公式: 在独立的行上用 `$$` 包裹

```
$$
\int_0^1 x^2 dx = \frac{1}{3}
$$
```

**保存状态**

右上角会显示保存状态。

| 状态 | 显示 |
|------|------|
| 已保存 | `已保存` |
| 未保存 | `未保存` |
| 保存中 | `保存中…` |

- 编辑器正文与预览正文的字体可在 设置 → 编辑器 中修改
- 在外部编辑器中修改笔记文件后，也会自动重新加载

**删除**

可通过垃圾桶图标删除笔记。若笔记处于置顶状态或存在未保存内容，则会显示警告。  
如果存在子笔记，将显示确认对话框，询问是否连同子笔记一起删除。

### 笔记界面的键盘快捷键

| 快捷键 | 动作 |
|----------------|------|
| `Ctrl+Enter` 或 `Ctrl+S` | 保存 |
| `Ctrl+1` | 跳转到 Cue 面板 |
| `Ctrl+2` | 跳转到 Note 面板 |
| `Ctrl+3` | 跳转到 Summary 面板 |
| `Ctrl+E` | 切换到编辑标签页 |
| `Ctrl+P` | 切换到预览标签页 |
| `/` | 聚焦到笔记搜索栏 |

---

## 工单界面

工单是用于管理任务和 ToDo 的功能。支持康奈尔式正文、状态管理、评论与备忘录。

### 左侧面板（工单列表）

**搜索・过滤**

| 操作 | 说明 |
|------|------|
| `/` 键 | 聚焦到搜索栏 |
| 优先级过滤 | 全部 / P0 / P1 / P2 / P3 |
| 排序 | 更新日期 / 截止日期 / 优先级 |

**新建**

- 点击 `新建` 按钮后会显示标题输入表单
- 输入标题后按 `Enter` 创建

**备忘录列表**

选择工单后，该工单中注册的备忘录会在下方以列表形式显示。

| 操作 | 说明 |
|------|------|
| 点击备忘录行 | 在右侧面板中打开该备忘录 |
| `👁` 图标 | 在列表中隐藏/重新显示备忘录 |
| `🗑` 图标 | 删除备忘录（带确认对话框） |

### 右侧面板（工单编辑器）

选择工单后，右侧面板会显示编辑器。

**元信息字段**

| 字段 | 说明 |
|------------|------|
| 标题 | 工单标题（必填） |
| 状态 | 自定义列（可在设置界面修改。例: backlog / doing / done） |
| 截止日期 | YYYY-MM-DD 格式 |
| 优先级 | P0（最高）/ P1 / P2 / P3（最低） |
| 标签 | 逗号分隔（例: urgent, ops） |

**标签页切换**

| 标签页 | 说明 |
|------|------|
| 编辑 | 康奈尔式编辑器（Cue / Note / Summary） |
| 预览 | Markdown 预览（康奈尔式 3 面板） |
| 评论（n） | 添加・查看评论 |
| 备忘录 | 查看并编辑当前工单的备忘录 |

**删除**

使用垃圾桶图标删除工单（带确认对话框）。  
若已注册备忘录，会显示包含备忘录数量的确认消息。

### 备忘录功能

工单可添加多个康奈尔式**备忘录**。可将会议记录、调查记录、补充资料与工单本体分开整理。

**添加备忘录**

- 点击 `备忘录` 标签页中的 `＋ 添加备忘录` 按钮，或左侧面板底部的同名按钮，即可立即创建
- 新建备忘录的默认标题为 `备忘录`

**备忘录编辑器**

备忘录编辑器采用与笔记相同的康奈尔布局，支持切换 1 / 2 / 3 面板显示。

| 标签页 | 说明 |
|------|------|
| 编辑 | 康奈尔式编辑器（Cue / Note / Summary） |
| 预览 | Markdown 预览（康奈尔 1 / 2 / 3 面板布局） |

- 输入标题后会立即开始自动保存
- 面板模式切换后会立即生效，并在下次打开时保持不变
- 预览标签页支持 PDF 导出
- 与笔记相同，在 Note 窗格正文上右键单击 → `添加便签` 即可放置便签

**备忘录排序**

可通过备忘录列表各行显示的 `▲` / `▼` 按钮更改备忘录的显示顺序。

**备忘录操作**

| 操作 | 说明 |
|------|------|
| 点击备忘录行 | 打开备忘录编辑器 |
| `👁` 图标 | 在列表中隐藏/重新显示备忘录（数据会保留） |
| `🗑` 图标 | 删除备忘录（带确认对话框） |
| `⛶`（全屏） | 以不显示列表面板的全屏视图打开备忘录 |
| `▲` / `▼` | 调整备忘录显示顺序 |

> 备忘录与工单绑定，删除工单时，备忘录也会一并删除。

### 工单界面的键盘快捷键

| 快捷键 | 动作 |
|----------------|------|
| `Ctrl+Enter` | 保存 |
| `/` | 聚焦到搜索栏 |

---

## 专注界面

这是一个按分数顺序显示重要笔记或工单，用于集中进行审阅与查看的界面。

### 显示模式

| 模式 | 说明 |
|--------|------|
| 笔记 | 按分数顺序显示笔记 |
| 工单 | 按分数顺序显示工单 |

### 评分

笔记分数由以下要素计算。

- **编辑次数**（过去 30 天）: 可在设置中切换 ON/OFF
- **被引用次数（incomingLinks）**: 被其他笔记引用的次数

### 界面构成

| 区域 | 内容 |
|------|------|
| 左侧列表 | 排名・标题・引用次数・更新日期 |
| 右侧预览 | 所选笔记的康奈尔式预览 |
| `最大` | 列表中显示的最大条数（5〜200） |

### 操作

| 操作 | 动作 |
|------|------|
| 点击 | 选择笔记并显示预览 |
| 双击 / `Enter` | 在笔记/工单界面中打开 |
| `全屏查看` 按钮 | 打开全屏查看器 |

### 全屏查看器

- 可在左侧列表中切换项目
- 可通过搜索栏搜索标题
- 使用 `打开` 按钮跳转到对应界面
- 使用 `ESC` 或 `关闭` 结束

### 代码片段搜索与固定

- 当搜索栏为空时，会以专注模式显示按编辑频率分数排序的列表
- 输入关键词后会切换为代码片段模式，并高亮显示匹配到的康奈尔面板
- 快速输入 `::` 可打开已固定代码片段的模态窗口并进行复制
- 可随时通过标题栏 `✂` 按钮或 `Ctrl+I` 打开代码片段管理对话框

### 专注界面的键盘快捷键

| 快捷键 | 动作 |
|----------------|------|
| `Enter` | 打开当前选中的项目 |
| `ESC` | 关闭全屏查看器 |

---

## OCR备忘录界面

可捕获屏幕上的任意区域并识别文字，自动保存为**OCR备忘录**，随后可进行查看与编辑。  
支持的识别语言包括日语、英语、中文、韩语、法语和德语。

### 开始 OCR 捕获

可通过以下任一方式开始捕获。

| 方法 | 操作 |
|------|------|
| 全局热键 | 按 `Ctrl+Shift+F12`（默认） |
| 系统托盘 | 右键单击托盘图标 → 选择 **OCR** |

开始捕获后，屏幕会变为半透明。用鼠标拖拽选择要识别的矩形区域后，即会执行 OCR 处理。

### OCR备忘录列表

点击标题栏中的 **OCR备忘录** 后，会显示列表界面。

| 列 | 内容 |
|----|------|
| 捕获时间 | 执行 OCR 的日期与时间 |
| 识别文本 | OCR 结果的开头部分 |

### OCR备忘录编辑界面

从列表中选择备忘录后，会显示编辑界面。

**图片区（当设置中“保存图片”为 ON 时）**

捕获的屏幕裁剪图会以固定在软木板背景上的卡片形式显示。

**文本区**

OCR 识别出的文本会显示在 Markdown 编辑器中。  
内容可自由编辑与修正。

| 操作 | 说明 |
|------|------|
| 编辑文本 | 直接在编辑器中修改 |
| 复制 | 将文本复制到剪贴板 |
| 删除 | 删除当前选中的备忘录（带确认对话框） |

### 批量删除

点击列表上方的 **全部删除** 按钮后，会显示确认对话框。  
确认后，所有 OCR备忘录 都会被删除。

> ⚠️ **此操作无法撤销。** 请事先复制好需要保留的文本。

---

## 设置界面

可通过标题栏中的 **设置** 修改各项设置。在左侧边栏中选择类别。

### 常规

| 设置 | 说明 |
|------|------|
| 语言 | UI 显示语言（system / ja / en / zh / ko / fr / de） |
| 区域设置 | 数值・日期格式的 locale（留空时使用 OS 设置，例: ja-JP） |
| 主题预设 | 配色主题 |

- `预览应用`: 立即预览所选主题
- `保存主题`: 将主题保存到 Host
- `保存语言/区域设置`: 保存语言设置
- 保存后的语言也会应用到文件对话框、启动错误提示以及 `Ctrl+F` 搜索面板等原生/内置界面

### 工作区

| 设置 | 说明 |
|------|------|
| WorkspaceRoot | 笔记与工单的保存路径 |

- `重新加载`: 重新获取当前 Workspace 设置
- `保存`: 保存修改后的路径

### 笔记

| 设置 | 说明 |
|------|------|
| 自动保存 | 输入时是否自动保存（ON/OFF） |
| 自动保存间隔（ms） | 自动保存间隔（200〜10,000ms，UI 本地设置） |

### 编辑器

| 设置 | 说明 |
|------|------|
| 字体族 | 用于编辑器正文与预览正文的字体 |
| 字体大小（px） | 正文字体大小 |
| 预览背景渲染 | 开启时渲染卡片/代码块等背景色；关闭时背景变为透明，PDF 导出也遵循此设置（默认：关闭） |
| 标题与表格使用主题色 | 开启时标题和表头反映主题强调色（主色）；关闭（默认）时统一为正文颜色，与 PDF 导出一致、更沉稳 |

- 输入中的设置在保存前也会立即预览
- `恢复默认值` 可将字体、预览背景渲染（OFF）和标题与表格使用主题色（OFF）还原为默认值

### 工单

| 设置 | 说明 |
|------|------|
| 自动保存 | 编辑工单时是否自动保存（ON/OFF） |
| 自动保存间隔（ms） | 自动保存间隔（UI 本地设置） |
| 列（Columns） | 工单状态候选项（逗号分隔）<br>例: `backlog, doing, done, blocked, archived` |
| 显示工单 ID | 在列表/预览等位置显示工单 ID（UI 本地设置） |

### 专注

| 设置 | 说明 |
|------|------|
| 最大条数 | 专注界面列表的最大条数 |
| 将编辑次数用于排名 | 将 note.save 的次数计入专注分数 |
| 活动日志（Activity） | 启用编辑次数统计（不会保存正文・搜索查询） |

> ※ 由 autoSave 触发的连续保存会按“每分钟 1 次”取整计数。

### 聚光灯

| 设置 | 说明 |
|------|------|
| 预设 1〜5 | 从 5 个固定聚光灯预设中选择要使用的方案 |
| 名称 | 各预设的显示名称（最多 20 个字符） |
| 大小（px） | 聚光灯圆形直径（20〜200px） |
| 颜色 | 聚光灯圆形与标题栏图标使用的颜色 |
| 透明度 | 聚光灯的透明程度 |

- 可通过标题栏 `☀` 按钮开启/关闭聚光灯
- `恢复默认值` 可将颜色与大小还原为默认值

### 快捷键

可修改全局热键（WPF 级别）。

| 快捷键 | 默认值 | 说明 |
|----------------|-----------|------|
| 显示 / 隐藏 | `Ctrl+Shift+F9` | 切换主窗口的显示与隐藏 |
| 聚光灯 | `Ctrl+Shift+F10` | 开启/关闭聚光灯光标 |
| 代码片段 | `Ctrl+Shift+F11` | 打开代码片段管理对话框 |
| OCR备忘录 | `Ctrl+Shift+F12` | 开始 OCR 捕获 |

**修改方法**

1. 点击要修改的快捷键输入框（会显示“点击后修改”）
2. 按下新的按键组合（必须包含修饰键）
3. 点击 `保存` 按钮确认
4. 点击 `重置` 按钮恢复默认值

保存设置后，系统托盘菜单中的热键显示也会立即更新。

### OCR

| 设置 | 说明 |
|------|------|
| 保存图片 | 在 OCR 捕获时将图片附加到备忘录中（默认: ON） |

- **ON**: 会保存 OCR 结果文本与捕获图片。OCR备忘录编辑界面会显示软木板风格的图片区。
- **OFF**: 仅保存文本。

### 维护

| 操作 | 说明 |
|------|------|
| **导出 Zip** | 将 Workspace（笔记、工单、OCR备忘录、图片）备份为 Zip 文件 |
| **从 Zip 恢复** | 从 Zip 恢复笔记、工单和 OCR备忘录（现有数据会被替换） |
| **打开日志文件夹** | 在文件管理器中打开应用日志文件夹 |
| **导出日志 Zip** | 以 Zip 格式导出日志 |
| **打开许可证文件夹** | 打开随附许可证文件所在文件夹 |

> ⚠️ **恢复操作会完全替换现有的笔记和工单。** 请务必先做好备份后再执行。

---

## 搜索语法参考

在主页的全文搜索栏中，可使用以下特殊语法。

| 语法 | 说明 | 例子 |
|------|------|----|
| 普通关键词 | 以空格分隔进行 AND 搜索 | `备忘录 会议` |
| `"短语"` | 短语搜索（将多个词作为一个整体搜索） | `"今天的工作"` |
| `has:url` | 搜索包含 URL 的笔记/工单 | `has:url` |
| `code:any` | 搜索包含代码块的项目 | `code:any` |
| `code:<语言>` | 搜索包含指定语言代码块的项目 | `code:powershell` |

**例:**
```
has:url code:powershell 备忘录
```
→ 包含 URL、包含 PowerShell 代码块，并包含“备忘录”这一关键词的项目

注意：快速输入 `::` 时，会打开已固定代码片段的模态窗口，而不会执行普通搜索。

---

## 快捷键一览

### 全局热键（WPF 级，可自定义）

可在 设置 → 快捷键 中修改。

| 快捷键（默认） | 动作 |
|------------------------------|------|
| `Ctrl+Shift+F9` | 主窗口显示 / 隐藏 |
| `Ctrl+Shift+F10` | 开启/关闭聚光灯光标 |
| `Ctrl+Shift+F11` | 打开代码片段管理对话框 |
| `Ctrl+Shift+F12` | 开始 OCR 捕获 |

### 应用内全局快捷键

| 快捷键 | 动作 |
|----------------|------|
| `Ctrl+K` | 聚焦到主页搜索栏 |
| `Ctrl+I` | 打开代码片段管理对话框 |
| `::` | 打开已固定代码片段的模态窗口 |
| `Ctrl+Shift+T` | 应用今天的日期过滤 |
| `Ctrl+鼠标滚轮` | 放大/缩小界面 |
| `Ctrl+0` | 将缩放重置为 100% |

### 笔记界面

| 快捷键 | 动作 |
|----------------|------|
| `Ctrl+Enter` / `Ctrl+S` | 保存 |
| `Ctrl+1` | 跳转到 Cue 面板 |
| `Ctrl+2` | 跳转到 Note 面板 |
| `Ctrl+3` | 跳转到 Summary 面板 |
| `Ctrl+E` | 切换到编辑标签页 |
| `Ctrl+P` | 切换到预览标签页 |
| `/` | 聚焦到搜索栏 |
| `Ctrl+F` | 打开编辑器内搜索面板 |

### 工单界面

| 快捷键 | 动作 |
|----------------|------|
| `Ctrl+Enter` | 保存 |
| `/` | 聚焦到搜索栏 |
| `Ctrl+F` | 打开编辑器内搜索面板 |

### 专注界面

| 快捷键 | 动作 |
|----------------|------|
| `Enter` | 打开当前选中的项目 |
| `ESC` | 关闭全屏查看器 |

### 主页（列表操作）

| 快捷键 | 动作 |
|----------------|------|
| `Enter` | 跳转到当前选中的项目 |
| `Ctrl+Click` | 跳转到当前选中的项目 |

---

## 常见问题（FAQ）

### Q. 专注分数是如何决定的？
**A.** 由以下要素计算。
- **编辑次数（edit30d）**: 过去 30 天的保存次数（连续自动保存会按每分钟 1 次取整）
- **被引用次数（incomingLinks）**: 被其他笔记链接的次数

可在 设置 → 专注 → **将编辑次数用于排名** 中切换 ON/OFF。

---

### Q. 可以给工单添加任意数量的备忘录吗？
**A.** 数量没有限制。可通过 `＋ 添加备忘录` 按钮添加。不再需要的备忘录可通过垃圾桶图标删除，如果只是想暂时隐藏，则可使用 👁 图标隐藏。

---

### Q. 如何删除 TODO？
**A.** TODO 是笔记中的 `- [ ] 文本` 格式行。编辑笔记时删除对应行，或将其标记为已完成，如 `- [x] 文本`。主页中的 TODO 列表会自动更新。

---

### Q. 我想新增或修改工单状态
**A.** 请在 设置 → 工单 → **列（Columns）** 中，以逗号分隔输入状态名称并保存。  
例: `backlog, doing, done, blocked, archived`

---

### Q. 可以在多台设备之间同步吗？
**A.** 通过将共享文件夹（NAS 或云同步文件夹）指定为 WorkspaceRoot，可以进行访问，但**由于同时编辑可能导致数据冲突，因此不推荐**。

---

### Q. 子笔记还能继续拥有子笔记吗？
**A.** 子笔记的嵌套限制为 1 层。在选中子笔记时按下 `＋ 子笔记`，会将其创建为与该父级相同父级的兄弟笔记（不会创建孙级笔记）。

---

### Q. OCR 可以识别哪些语言？
**A.** 支持日语、英语、中文、韩语、法语和德语。即使屏幕文本中混合了多种语言也可以识别，但精度会因语言和字体大小而异。

---

### Q. 我不想保存 OCR 捕获的图片
**A.** 将 设置 → OCR → **保存图片** 设为 OFF 后，即只会保存文本。已附加到现有备忘录中的图片不会受到影响。

---

### Q. 我想修改全局热键
**A.** 可在 设置 → 快捷键 中分别修改 4 个热键。点击输入框后按下新的按键组合，再点击 `保存` 即可。

---

### Q. 自动保存和置顶可以同时使用吗？
**A.** 可以。自动保存会定期保存笔记正文与元信息，而置顶状态会通过另一套机制保存。

---

## 故障排除

### 应用无法启动
1. 确认 **WebView2 Runtime** 是否已安装
2. 确认 Host 端（后端进程）是否正常启动
3. 通过 设置 → 维护 → **打开日志文件夹** 查看错误日志

### 无法读取数据 / 显示错误

| 错误 | 处理方法 |
|--------|------|
| `E_WS_NOT_WRITABLE` | 确认 Workspace 文件夹的写入权限 |
| `E_WS_INVALID_ROOT` | 在 设置 → 工作区 中确认并修正路径 |
| `E_APP_NOT_READY` | 等待启动完成后再重试 |
| `E_WEBVIEW2_UNAVAILABLE` | 安装 WebView2 Runtime |
| `E_SETTINGS_CORRUPT` | 设置将被自动初始化。请重新进行设置 |

文件读写错误与原生对话框消息会按照设置中选择的显示语言显示。

### 自动保存不起作用
1. 确认 设置 → 笔记/工单 → **自动保存** 是否已启用
2. 确认 autoSaveMs 是否在 `200〜10,000` 范围内
3. 确认笔记/工单内容是否确实发生了变更（无变更时不会保存）

### 搜索结果不显示
1. 检查搜索词拼写
2. 检查是否启用了标签过滤或日期过滤（点击过滤 Chip 上的 `×` 可解除）
3. 将过滤器切换为 **全部** 后重新搜索

### Peek 预览不显示
- 点击项目时请使用**单击**，而不是双击
- 如果窗口宽度过窄，请加宽窗口以显示右侧面板

### OCR 无法工作
1. OCR 引擎初始化可能需要几秒钟，请稍等后再试
2. 重启应用后重新尝试
3. 通过 设置 → 维护 → **打开日志文件夹** 查看错误日志

# CattitudeFlow ユーザーマニュアル

## 目次

1. [概要](#概要)
2. [画面構成](#画面構成)
3. [ホーム画面](#ホーム画面)
4. [ノート画面](#ノート画面)
5. [チケット画面](#チケット画面)
6. [フォーカス画面](#フォーカス画面)
7. [OCRメモ画面](#ocr-メモ画面)
8. [設定画面](#設定画面)
9. [検索構文リファレンス](#検索構文リファレンス)
10. [ショートカット一覧](#ショートカット一覧)
11. [よくある質問（FAQ）](#よくある質問faq)
12. [トラブルシューティング](#トラブルシューティング)

---

## 概要

**CattitudeFlow** は、コーネル式ノートとチケット管理を組み合わせたデスクトップアプリです。

ノートの本文はMarkdown形式で記述でき、Cue（キュー）・Note（ノート）・Summary（要約）の3ペインで構成されるコーネル式フォーマットをサポートしています。  
チケットにはコーネル式の**メモ**を複数追加でき、タスクに関する詳細な記録を残すことができます。  
v1.0.5 では、画面上のテキストをそのままアプリに取り込める **OCRキャプチャ** 機能に加え、本文上に自由配置できる **付箋メモ**、画面共有向けの **プレゼン表示モード**、**Markdown数式（KaTeX）** などの機能が追加されました。

---

## 画面構成

### ヘッダーナビゲーション

アプリ上部のヘッダーに以下のナビゲーションボタンが表示されます。

| ボタン | 説明 |
|--------|------|
| ホーム | ノート・チケット・TODOの一覧と検索 |
| ノート | コーネル式ノートの作成・編集 |
| チケット | タスク・チケットの管理 |
| フォーカス | 重要度スコアによるノート/チケット一覧 |
| OCRメモ | OCRキャプチャで取り込んだメモの一覧と編集 |
| 設定 | アプリ設定 |

### InfoPill（情報ピル）

ヘッダー右側に以下の情報が表示されます。

- **残り日数**: 試用期間の残日数（例: 残り7日）
- **空き容量**: ディスク空き容量の状態（OK/少）

### ウィンドウ操作ボタン

ネイティブアプリとして起動時、ヘッダー右端に表示されます。

| ボタン | 操作 |
|--------|------|
| `_` | 最小化 |
| `□` | 最大化/元に戻す |
| `×` | 閉じる |

### タイトルバーのクイックボタン

| ボタン | 操作 |
|--------|------|
| `✂` | スニペット管理ダイアログを開く（`Ctrl+I`） |
| `☀` | スポットライトカーソルの ON/OFF |
| `☀ ▾` | 現在のスポットライトプリセットを切り替える |
| `🖥` プレゼン表示 | ライブプレビュー/プレビューの可視性を高める **プレゼン表示モード** を ON/OFF（画面共有向け） |

スニペットボタンには登録件数のバッジが表示されます。スポットライトボタンの色は、現在選択中のプリセット色に連動します。

**プレゼン表示モード** は、ライブプレビューやプレビューの文字サイズ・行間・コントラストを強調し、画面共有時でも見やすくします。ON/OFF はアプリ内に記憶され、テーマ配色や保存中のデータには影響しません。

### タスクトレイメニュー

タスクトレイのアイコンを右クリックすると以下のメニューが表示されます。

| 項目 | 説明 |
|------|------|
| 表示 / 非表示 | メインウィンドウの表示・非表示を切り替える |
| スポットライト | スポットライトカーソルの ON/OFF |
| スニペット | スニペット管理ダイアログを開く |
| OCR | 画面キャプチャを開始する |
| 終了 | アプリを終了する |

各項目の右側に現在設定されているグローバルホットキーが表示されます。  
ホットキーは 設定 → ショートカットキー でカスタマイズできます。

---

## ホーム画面

ホーム画面は3列構成で、左メニュー・中央リスト・右Peekプレビューで構成されます。

### 左パネル（メニュー）

**フィルタリスト**

| 項目 | 説明 |
|------|------|
| ノート | ノートのみ表示 |
| チケット | チケットのみ表示 |
| TODO | 全ノートのTODOアイテムを表示 |
| イメージ | 添付画像の一覧表示 |
| すべて | ノート + チケット + TODOをまとめて表示 |

各項目には件数が表示されます。タグを選択中の場合は `タグ件数 / 合計件数` の形式で表示されます。

**タグフィルター**

ノートとチケットのタグが統合されてチップ形式で表示されます。  
タグをクリックするとそのタグを持つ項目のみ表示されます。`すべて` をクリックするとフィルターが解除されます。

### 中央パネル（リスト）

**検索バー**

- プレースホルダー: `全文検索（Ctrl+K） 例: has:url code:powershell foo`
- `Ctrl+K` でフォーカス
- `クリア` ボタンで検索ワードをリセット
- `::` をすばやく入力すると、ピン留めしたスニペットのモーダルを開けます

**フィルターチップ**

検索バーの下にフィルターチップが表示されます（すべて / ノート / チケット / TODO）。  
TODO選択時は **スコープ** チップも表示されます（すべて / 本日）。  
日付フィルターが有効な場合は `date: YYYY-MM-DD ×` チップが表示され、クリックで解除できます。

**リスト操作**

| 操作 | 動作 |
|------|------|
| シングルクリック | 右ペインにPeekプレビュー表示 |
| ダブルクリック | 対応するノート/チケット画面に移動 |
| `Ctrl+Click` | 対応する画面に移動 |
| `Enter` | 対応する画面に移動 |

**チケットのメモ行**

チケットにメモが登録されている場合、チケット行の下にメモが子行として表示されます。
メモを持つチケットには `▼ / ▶` トグルが表示され、メモツリーを折りたたみ/展開できます。

| 操作 | 動作 |
|------|------|
| シングルクリック | チケット画面に移動してメモを開く |
| `⛶`（全画面ボタン） | メモをリストなしの全画面プレビューで表示 |

**イメージタブ選択時**

- 画像のサムネイル・パス・作成日・サイズ・参照元件数が表示されます
- 専用の検索バー（uid / relPathで検索）が表示されます
- リストの末尾までスクロールで追加ロードが可能です

### 右パネル（Peekプレビュー）

項目をシングルクリックすると右ペインにプレビューが表示されます。

- ノート: コーネル式プレビュー（Cue / Note / Summary）
- チケット: チケット情報のプレビュー
- 画像: 画像プレビューと参照元ノート/チケット一覧
- チケットのメモ: メモのコーネル式プレビュー（リストパネルなし）
- **全画面ボタン** (⛶): プレビューを全画面表示
- **縮小/拡大**: コーネル式プレビューのサイズ切替（1枚表示 / 読みやすい）
- 外部エディタで変更したファイルも自動検知され、一覧とプレビューに反映されます

### ホーム画面のキーボードショートカット

| ショートカット | 動作 |
|----------------|------|
| `Ctrl+K` | 検索バーにフォーカス |
| `Ctrl+Shift+T` | 本日の日付フィルターを適用して今月カレンダーを表示 |

---

## ノート画面

### 画面構成

ノート画面は**左リスト**と**右エディター**の2列構成です。

### 左パネル（ノートリスト）

**新規作成**

- `新規` ボタンまたは `+` から新規ノートダイアログを開きます
- ダイアログでタイトルと日付（デフォルト: 本日）を入力して作成
- 同名タイトルのノートが既存の場合は既存ノートが開きます

**子ノート作成**

- ノート選択中に `＋ 子ノート` ボタンをクリックすると、選択中のノートの子ノートを作成します
- 子ノートはリストでインデントされた行として表示されます
- 現在開いているノートがすでに子ノートの場合は、同じ親を持つ兄弟ノートとして作成されます（孫ノートは作成されません）

**検索**

- `/` キーで検索バーにフォーカス
- HostのFull-text searchによるノート内容の全文検索が可能

**タグフィルター**

タグチップをクリックしてタグでノートを絞り込めます。

**ピン留め**

- ノート行の★アイコンでピン留めトグル
- ピン留めされたノートはリスト最上部に表示
- `ピン済み` と `すべて` のセクションに分かれて表示
- ピン留めはWorkspaceに永続保存されます（最大100件）

### 右パネル（エディター）

**メタ情報フィールド**

| フィールド | 説明 |
|------------|------|
| タイトル | ノートのタイトル（必須） |
| 日付 | YYYY-MM-DD形式（例: 2026-04-26） |
| タグ | カンマ区切り（例: ops, daily） |

**コーネル式エディター**

ノート本文は以下の3ペインで構成されます。

| ペイン | 用途 |
|--------|------|
| **Cue**（キュー） | 質問・キーワード・ヒント |
| **Note**（ノート） | 主要な内容・詳細メモ |
| **Summary**（要約） | まとめ・結論 |

**ペイン表示モード**

ペインモードトグルで表示モードを切り替えられます。

| モード | 表示 |
|--------|------|
| `1` | 1ペイン（アクティブペインのみ表示） |
| `2` | 2ペイン |
| `3` | 3ペイン（全ペイン同時表示）※デフォルト |

切り替えたモードは即時に反映され、再度ノートを開いたときも前回の表示モードが復元されます。

**タブ切り替え**

| タブ | 説明 |
|------|------|
| 編集 | CodeMirrorエディター |
| プレビュー | Markdownプレビュー（目次付き） |

- 編集中に `Ctrl+F` でアプリの表示言語に合わせた検索パネルを開けます
- プレビュータブでは現在の背景色を反映した PDF エクスポートが利用できます

**Markdownツールバー**

編集タブ上部のツールバーで各種書式を挿入できます。

| ボタン | 書式 |
|--------|------|
| ボールド | `**テキスト**` |
| イタリック | `*テキスト*` |
| 打ち消し線 | `~~テキスト~~` |
| 下線 | `<u>テキスト</u>` |
| 文字色 | `<span style="color:#XXXXXX">テキスト</span>`（20色） |
| 箇条書き | `- テキスト` |
| 番号付きリスト | `1. テキスト` |
| TODOリスト | `- [ ] テキスト` |
| テーブル | テーブル雛形挿入 |
| アラートボックス | GFMアラート（NOTE / TIP / IMPORTANT / WARNING / CAUTION） |
| 見出し | H1 / H2 / H3 |

**文字色パレット（20色）**

Red / Crimson / Orange / Amber / Yellow / Lime / Green / Teal / Cyan / Sky /  
Blue / Indigo / Purple / Violet / Pink / Rose / Brown / Slate / Gray / Black

**画像のペースト**

クリップボードの画像をエディターに貼り付けると、自動でWorkspaceに保存しMarkdownの画像記法で挿入されます。  
取り込み中は「画像を取り込み中…」のオーバーレイが表示されます。

**付箋メモ（ふせん）**

ノート本文の上に、自由配置できる付箋メモを貼り付けられます。エディター本文上で右クリックし、コンテキストメニューの `付箋を追加` を選びます。

- 追加した付箋には自動でフォーカスが移り、そのまま入力を始められます（入力中にフォーカスを奪われません）
- 上部のバーをドラッグして移動できます。付箋は本文の位置にアンカーされ、スクロールや編集に追従します
- 左上の色ボタンで色を切り替えます（黄 → 桃 → 青 → 緑 の順に循環）
- テキストエリア右下をドラッグするとサイズを変更でき、変更後のサイズは保存されます
- `✕` ボタンで削除します（確認ダイアログあり）
- `Ctrl+;` で日付、`Ctrl+Shift+;` で時刻を挿入できます
- 付箋はノート本文に保存されるため、Zipバックアップにも含まれます

**数式（Math）**

Markdownプレビューおよびライブプレビューは、KaTeX による数式表示に対応しています。

- インライン数式: `$E = mc^2$`
- ブロック数式: 独立した行に `$$` で囲んで記述します

```
$$
\int_0^1 x^2 dx = \frac{1}{3}
$$
```

**保存ステータス**

右上に保存状態が表示されます。

| 状態 | 表示 |
|------|------|
| 保存済み | `保存済み` |
| 未保存 | `未保存` |
| 保存中 | `保存中…` |

- エディター本文とプレビュー本文のフォントは 設定 → エディタ で変更できます
- 外部エディタでノートファイルを更新した場合も、自動で再読み込みされます

**削除**

ゴミ箱アイコンからノートを削除できます。ピン留め中・未保存の場合は警告が表示されます。  
子ノートが存在する場合は、子ノートも含めて削除するか確認ダイアログが表示されます。

### ノート画面のキーボードショートカット

| ショートカット | 動作 |
|----------------|------|
| `Ctrl+Enter` または `Ctrl+S` | 保存 |
| `Ctrl+1` | Cueペインにジャンプ |
| `Ctrl+2` | Noteペインにジャンプ |
| `Ctrl+3` | Summaryペインにジャンプ |
| `Ctrl+E` | 編集タブに切り替え |
| `Ctrl+P` | プレビュータブに切り替え |
| `/` | ノート検索バーにフォーカス |

---

## チケット画面

チケットはタスクやToDoを管理するための機能です。コーネル式の本文・ステータス管理・コメント・メモをサポートします。

### 左パネル（チケットリスト）

**検索・フィルタリング**

| 操作 | 説明 |
|------|------|
| `/` キー | 検索バーにフォーカス |
| 優先度フィルター | 全て / P0 / P1 / P2 / P3 |
| 並び替え | 更新日 / 期限 / 優先度 |

**新規作成**

- `新規` ボタンでタイトル入力フォームが表示されます
- タイトルを入力して `Enter` で作成

**メモリスト**

チケットを選択すると、そのチケットに登録されているメモが下部にリスト表示されます。

| 操作 | 説明 |
|------|------|
| メモ行をクリック | そのメモを右パネルで開く |
| `👁` アイコン | メモをリストから非表示/再表示 |
| `🗑` アイコン | メモを削除（確認ダイアログあり） |

### 右パネル（チケットエディター）

チケットを選択すると右パネルにエディターが表示されます。

**メタ情報フィールド**

| フィールド | 説明 |
|------------|------|
| タイトル | チケットのタイトル（必須） |
| ステータス | カスタム列（設定画面で変更可。例: backlog / doing / done） |
| 期限 | YYYY-MM-DD形式 |
| 優先度 | P0（最高）/ P1 / P2 / P3（最低） |
| ラベル | カンマ区切り（例: urgent, ops） |

**タブ切り替え**

| タブ | 説明 |
|------|------|
| 編集 | コーネル式エディター（Cue / Note / Summary） |
| プレビュー | Markdownプレビュー（コーネル式3ペイン） |
| コメント（n） | コメントの追加・閲覧 |
| メモ | 選択中チケットのメモ一覧と編集 |

**削除**

ゴミ箱アイコンでチケットを削除します（確認ダイアログあり）。  
メモが登録されている場合は、メモの件数を含む確認メッセージが表示されます。

### メモ機能

チケットには複数のコーネル式**メモ**を追加できます。会議メモ・調査記録・補足資料を、チケット本体とは分けて整理できます。

**メモの追加**

- `メモ` タブ内の `＋ メモ追加` ボタン、または左パネル下部の同名ボタンから即時作成できます
- 新規メモのデフォルトタイトルは `メモ` です

**メモエディター**

メモのエディターはノートと同じ Cornell レイアウトを採用しており、1 / 2 / 3 ペイン表示を切り替えられます。

| タブ | 説明 |
|------|------|
| 編集 | コーネル式エディター（Cue / Note / Summary） |
| プレビュー | Markdownプレビュー（Cornell 1 / 2 / 3 ペイン表示） |

- タイトル入力後すぐに自動保存が開始されます
- ペインモードは切り替え後すぐに反映され、次回も維持されます
- プレビュータブでは PDF エクスポートを利用できます
- ノートと同様に、Note ペイン本文上で右クリック → `付箋を追加` で付箋メモを貼り付けられます

**メモの並び替え**

メモリストの各行に表示される `▲` / `▼` ボタンで、メモの並び順を変更できます。

**メモの操作**

| 操作 | 説明 |
|------|------|
| メモ行をクリック | メモエディターを開く |
| `👁` アイコン | メモをリストから非表示/再表示（データは保持） |
| `🗑` アイコン | メモを削除（確認ダイアログあり） |
| `⛶`（全画面） | リストパネルなしの全画面表示でメモを開く |
| `▲` / `▼` | メモの表示順を変更する |

> メモはチケットに紐づいており、チケットを削除するとメモも合わせて削除されます。

### チケット画面のキーボードショートカット

| ショートカット | 動作 |
|----------------|------|
| `Ctrl+Enter` | 保存 |
| `/` | 検索バーにフォーカス |

---

## フォーカス画面

重要なノートやチケットをスコア順に表示し、集中的にレビュー・閲覧するための画面です。

### 表示モード

| モード | 説明 |
|--------|------|
| ノート | ノートをスコア順に表示 |
| チケット | チケットをスコア順に表示 |

### スコアリング

ノートのスコアは以下の要素で計算されます。

- **編集回数**（過去30日）: 設定でON/OFFを切り替え可能
- **被参照数（incomingLinks）**: 他のノートから参照されている件数

### 画面構成

| 領域 | 内容 |
|------|------|
| 左リスト | 順位・タイトル・参照数・更新日 |
| 右プレビュー | 選択ノートのコーネル式プレビュー |
| `最大` | 一覧に表示する最大件数（5〜200） |

### 操作

| 操作 | 動作 |
|------|------|
| クリック | ノートを選択してプレビュー表示 |
| ダブルクリック / `Enter` | ノート/チケット画面で開く |
| `全画面閲覧` ボタン | 全画面ビューアーを開く |

### 全画面ビューアー

- 左リストで項目を切り替え可能
- 検索バーでタイトル検索が可能
- `開く` ボタンで対応画面に移動
- `ESC` または `閉じる` で終了

### スニペット検索とピン留め

- 検索バーが空のときは Focus モードとして、編集頻度スコア順の一覧を表示します
- キーワードを入力すると Snippet モードに切り替わり、一致した Cornell ペインだけを強調表示します
- `::` をすばやく入力すると、ピン留め済みスニペットのモーダルを開いてコピーできます
- タイトルバーの `✂` ボタンまたは `Ctrl+I` で、スニペット管理ダイアログをいつでも開けます

### フォーカス画面のキーボードショートカット

| ショートカット | 動作 |
|----------------|------|
| `Enter` | 選択中の項目を開く |
| `ESC` | 全画面ビューアーを閉じる |

---

## OCR メモ画面

画面上の任意の領域をキャプチャしてテキストを認識し、**OCRメモ**として自動保存・閲覧・編集できます。  
認識言語は日本語・英語・中国語・韓国語・フランス語・ドイツ語に対応しています。

### OCRキャプチャの開始

以下のいずれかの方法でキャプチャを開始します。

| 方法 | 操作 |
|------|------|
| グローバルホットキー | `Ctrl+Shift+F12`（デフォルト）を押す |
| タスクトレイ | トレイアイコンを右クリック → **OCR** を選択 |

キャプチャ開始後、画面が半透明になります。マウスをドラッグして認識したい矩形領域を選択するとOCR処理が実行されます。

### OCRメモの一覧

ヘッダーの **OCRメモ** をクリックすると一覧画面が表示されます。

| 列 | 内容 |
|----|------|
| キャプチャ日時 | OCRを実行した日時 |
| 認識テキスト | OCR結果の先頭部分 |

### OCRメモの編集画面

一覧からメモを選択すると編集画面が表示されます。

**画像エリア（設定で「画像を保存」がONの場合）**

キャプチャした画面の切り抜き画像がコルク背景にピン留めされたカードとして表示されます。

**テキストエリア**

OCRで認識されたテキストがMarkdownエディターに表示されます。  
内容は自由に編集・修正できます。

| 操作 | 説明 |
|------|------|
| テキスト編集 | エディターで直接修正 |
| コピー | テキストをクリップボードにコピー |
| 削除 | 選択中のメモを削除（確認ダイアログあり） |

### 一括削除

リスト上部の **すべて削除** ボタンをクリックすると確認ダイアログが表示されます。  
確認後、すべてのOCRメモが削除されます。

> ⚠️ **この操作は取り消せません。** 必要なテキストは事前にコピーしてください。

---

## 設定画面

ヘッダーの **設定** から各種設定を変更できます。左サイドバーでカテゴリを選択します。

### 一般（General）

| 設定 | 説明 |
|------|------|
| 言語 | UI表示言語（system / ja / en / zh / ko / fr / de） |
| ロケール | 数値・日付フォーマットのロケール（空欄でOS設定を使用、例: ja-JP） |
| テーマプリセット | 配色テーマ |

- `プレビュー適用`: 選択テーマを即時プレビュー
- `テーマを保存`: テーマをHostに保存
- `言語/ロケールを保存`: 言語設定を保存
- 保存した言語は、ファイルダイアログ・起動時エラー・`Ctrl+F` 検索パネルなどのネイティブ/組み込み UI にも反映されます

### Workspace

| 設定 | 説明 |
|------|------|
| WorkspaceRoot | ノートとチケットの保存先パス |

- `再読み込み`: 現在のWorkspace設定を再取得
- `保存`: 変更したパスを保存

### ノート

| 設定 | 説明 |
|------|------|
| 自動保存 | 入力時に自動保存するか（ON/OFF） |
| 自動保存間隔（ms） | 自動保存のインターバル（200〜10,000ms、UIローカル設定） |

### エディタ

| 設定 | 説明 |
|------|------|
| フォントファミリー | エディター本文とプレビュー本文に使うフォント |
| フォントサイズ（px） | 本文フォントサイズ |
| プレビュー背景描画 | ON でカード・コードブロック等の背景色を描画。OFF にすると背景が透明になり、PDF出力時もこの設定が適用されます（既定: OFF） |
| 見出し・表にテーマ色を使う | ON で見出しや表ヘッダにテーマの配色（プライマリ色）を反映。OFF（既定）では本文色に統一され、PDF出力と同じ落ち着いた配色になります |

- 入力中の値は保存前でも即時プレビューされます
- `初期値に戻す` で既定値（フォント・背景描画OFF・テーマ色OFF）に戻せます

### チケット

| 設定 | 説明 |
|------|------|
| 自動保存 | チケット編集時に自動保存するか（ON/OFF） |
| 自動保存間隔（ms） | 自動保存のインターバル（UIローカル設定） |
| 列（Columns） | チケットのStatus候補（カンマ区切り）<br>例: `backlog, doing, done, blocked, archived` |
| チケットID表示 | 一覧/プレビュー等にチケットIDを表示（UIローカル設定） |

### フォーカス

| 設定 | 説明 |
|------|------|
| 最大件数 | フォーカス画面のリスト最大件数 |
| 編集回数を順位に使う | note.saveの回数をFocusスコアに加点する |
| 利用状況ログ（Activity） | 編集回数の集計を有効化（本文・検索クエリは保存されません） |

> ※ autoSaveによる連続保存は「1分に1回」に丸めてカウントされます。

### スポットライト

| 設定 | 説明 |
|------|------|
| プリセット 1〜5 | 5つの固定プリセットから使用するスポットライトを選択 |
| 名前 | 各プリセットの表示名（最大20文字） |
| サイズ（px） | スポットライト円の直径（20〜200px） |
| 色 | スポットライト円とヘッダーアイコンの色 |
| 透明度 | スポットライトの透過度 |

- タイトルバーの `☀` ボタンで ON/OFF を切り替えられます
- `↩ 初期値に戻す` で色とサイズを既定値に戻せます

### ショートカットキー

グローバルホットキー（WPFレベル）を変更できます。

| ショートカット | デフォルト | 説明 |
|----------------|-----------|------|
| 表示 / 非表示 | `Ctrl+Shift+F9` | メインウィンドウの表示・非表示を切り替える |
| スポットライト | `Ctrl+Shift+F10` | スポットライトカーソルの ON/OFF |
| スニペット | `Ctrl+Shift+F11` | スニペット管理ダイアログを開く |
| OCR メモ | `Ctrl+Shift+F12` | OCRキャプチャを開始する |

**変更方法**

1. 変更したいショートカットの入力欄をクリック（「クリックして変更」と表示）
2. 新しいキーの組み合わせを押す（修飾キーが必要）
3. `保存` ボタンで確定
4. `リセット` ボタンでデフォルトに戻す

設定を保存すると、タスクトレイメニューのホットキー表示にも即時反映されます。

### OCR

| 設定 | 説明 |
|------|------|
| 画像を保存 | OCRキャプチャ時に画像をメモに添付する（デフォルト: ON） |

- **ON**: OCR結果のテキストと共にキャプチャ画像を保存します。OCRメモ編集画面にコルクボード風の画像エリアが表示されます。
- **OFF**: テキストのみを保存します。

### メンテナンス

| 操作 | 説明 |
|------|------|
| **Zipを書き出す** | Workspace（ノート・チケット・OCRメモ・画像）をZipファイルとしてバックアップ |
| **Zipから復元** | Zipからノート・チケット・OCRメモを復元（既存データは置換されます） |
| **ログフォルダを開く** | アプリログのフォルダをファイルマネージャーで開く |
| **ログZipを出力** | ログをZip形式でエクスポート |
| **ライセンスフォルダを開く** | 同梱ライセンスファイルのフォルダを開く |

> ⚠️ **復元操作は既存のノートとチケットを完全に置換します。** 必ずバックアップを取ってから実行してください。

---

## 検索構文リファレンス

ホーム画面の全文検索バーでは以下の特殊構文が使用できます。

| 構文 | 説明 | 例 |
|------|------|----|
| 通常キーワード | スペース区切りでAND検索 | `メモ 会議` |
| `"フレーズ"` | フレーズ検索（複数語を一塊として検索） | `"今日の作業"` |
| `has:url` | URLを含むノート/チケットを検索 | `has:url` |
| `code:any` | コードブロックを含む項目を検索 | `code:any` |
| `code:<言語>` | 指定言語のコードブロックを含む項目を検索 | `code:powershell` |

**例:**
```
has:url code:powershell メモ
```
→ URLを含み、PowerShellのコードブロックを含み、「メモ」というキーワードを含む項目

※ `::` をすばやく入力すると、検索結果の代わりにピン留め済みスニペットモーダルを開けます。

---

## ショートカット一覧

### グローバルホットキー（WPFレベル・カスタマイズ可能）

設定 → ショートカットキー で変更できます。

| ショートカット（デフォルト） | 動作 |
|------------------------------|------|
| `Ctrl+Shift+F9` | メインウィンドウの表示 / 非表示 |
| `Ctrl+Shift+F10` | スポットライトカーソルを ON/OFF |
| `Ctrl+Shift+F11` | スニペット管理ダイアログを開く |
| `Ctrl+Shift+F12` | OCRキャプチャを開始する |

### アプリ内グローバル

| ショートカット | 動作 |
|----------------|------|
| `Ctrl+K` | ホーム検索バーにフォーカス |
| `Ctrl+I` | スニペット管理ダイアログを開く |
| `::` | ピン留め済みスニペットモーダルを開く |
| `Ctrl+Shift+T` | 本日の日付フィルターを適用 |
| `Ctrl+マウスホイール` | 画面をズームイン/ズームアウト |
| `Ctrl+0` | ズームを100%にリセット |

### ノート画面

| ショートカット | 動作 |
|----------------|------|
| `Ctrl+Enter` / `Ctrl+S` | 保存 |
| `Ctrl+1` | Cueペインにジャンプ |
| `Ctrl+2` | Noteペインにジャンプ |
| `Ctrl+3` | Summaryペインにジャンプ |
| `Ctrl+E` | 編集タブに切り替え |
| `Ctrl+P` | プレビュータブに切り替え |
| `/` | 検索バーにフォーカス |
| `Ctrl+F` | エディター内検索パネルを開く |

### チケット画面

| ショートカット | 動作 |
|----------------|------|
| `Ctrl+Enter` | 保存 |
| `/` | 検索バーにフォーカス |
| `Ctrl+F` | エディター内検索パネルを開く |

### フォーカス画面

| ショートカット | 動作 |
|----------------|------|
| `Enter` | 選択中の項目を開く |
| `ESC` | 全画面ビューアーを閉じる |

### ホーム画面（リスト操作）

| ショートカット | 動作 |
|----------------|------|
| `Enter` | 選択中の項目に移動 |
| `Ctrl+Click` | 選択中の項目に移動 |

---

## よくある質問（FAQ）

### Q. フォーカスのスコアはどう決まる？
**A.** 以下の要素で計算されます。
- **編集回数（edit30d）**: 過去30日の保存回数（連続自動保存は1分に1回に丸め）
- **被参照数（incomingLinks）**: 他のノートからリンクされている件数

設定 → フォーカス → **編集回数を順位に使う** でON/OFFを切り替えられます。

---

### Q. チケットにメモを何件でも追加できる？
**A.** 件数に制限はありません。`＋ メモ追加` ボタンで追加できます。不要になったメモはゴミ箱アイコンで削除、一時的に隠したい場合は👁アイコンで非表示にできます。

---

### Q. TODOの削除はどうする？
**A.** TODOはノート内の `- [ ] テキスト` 形式の行です。ノート編集時に該当行を削除するか、`- [x] テキスト` のようにチェック済みにしてください。ホーム画面のTODOリストは自動的に更新されます。

---

### Q. チケットのステータスを追加・変更したい
**A.** 設定 → チケット → **列（Columns）** にカンマ区切りでステータス名を入力して保存してください。  
例: `backlog, doing, done, blocked, archived`

---

### Q. 複数端末での同期はできる？
**A.** WorkspaceRootに共有フォルダ（NASやクラウド同期フォルダ）を指定することで参照は可能ですが、**同時編集はデータ競合の可能性があるため非推奨**です。

---

### Q. 子ノートはさらに子ノートを持てる？
**A.** 子ノートはネストを1段階に制限しています。子ノートを選択中に `＋ 子ノート` を押すと、その親と同じ親を持つ兄弟ノートとして作成されます（孫ノートは作成されません）。

---

### Q. OCRが認識できる言語は？
**A.** 日本語・英語・中国語・韓国語・フランス語・ドイツ語に対応しています。画面内のテキストが複数言語混在している場合も認識できますが、精度は言語や文字サイズによって異なります。

---

### Q. OCRキャプチャの画像を保存したくない
**A.** 設定 → OCR → **画像を保存** を OFF にすると、テキストのみが保存されます。既存のメモに添付された画像には影響しません。

---

### Q. グローバルホットキーを変更したい
**A.** 設定 → ショートカットキー で4つのホットキーをそれぞれ変更できます。入力欄をクリックして新しいキー組み合わせを押し、`保存` をクリックしてください。

---

### Q. 自動保存とピン留めは同時に使える？
**A.** はい。自動保存はノート本文・メタ情報を定期保存し、ピン留め状態は別のメカニズムで保存されます。

---

## トラブルシューティング

### アプリが起動しない
1. **WebView2 Runtime** がインストールされているか確認する
2. Host側（バックエンドプロセス）が正常に起動しているか確認する
3. 設定 → メンテナンス → **ログフォルダを開く** でエラーログを確認する

### データが読み込めない / エラーが表示される

| エラー | 対処 |
|--------|------|
| `E_WS_NOT_WRITABLE` | Workspaceフォルダの書き込み権限を確認 |
| `E_WS_INVALID_ROOT` | 設定 → Workspace でパスを確認・修正 |
| `E_APP_NOT_READY` | 起動完了を待ってから再試行 |
| `E_WEBVIEW2_UNAVAILABLE` | WebView2 Runtimeをインストール |
| `E_SETTINGS_CORRUPT` | 設定が自動初期化されます。再度設定を行ってください |

ファイル読み書きエラーやネイティブダイアログのメッセージは、設定した表示言語で表示されます。

### 自動保存が動作しない
1. 設定 → ノート/チケット → **自動保存** が有効か確認
2. autoSaveMs が `200〜10,000` の範囲内か確認
3. ノート/チケットの内容が実際に変更されているか確認（無変更時は保存しません）

### 検索結果が表示されない
1. 検索ワードのスペルを確認
2. タグフィルターや日付フィルターが適用されていないか確認（フィルターチップの `×` で解除）
3. フィルターを **すべて** に切り替えて再検索

### Peekプレビューが表示されない
- ダブルクリックではなく**シングルクリック**で項目をクリックする
- ウィンドウ幅が狭い場合は広げて右ペインを表示させる

### OCRが動作しない
1. OCRエンジンの初期化に数秒かかる場合があります。しばらく待ってから再試行してください
2. アプリを再起動して再試行してください
3. 設定 → メンテナンス → **ログフォルダを開く** でエラーログを確認してください

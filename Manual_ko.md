# CattitudeFlow 사용자 매뉴얼

## 목차

1. [개요](#개요)
2. [화면 구성](#화면-구성)
3. [홈 화면](#홈-화면)
4. [노트 화면](#노트-화면)
5. [티켓 화면](#티켓-화면)
6. [포커스 화면](#포커스-화면)
7. [OCR 메모 화면](#ocr-메모-화면)
8. [설정 화면](#설정-화면)
9. [검색 구문 레퍼런스](#검색-구문-레퍼런스)
10. [단축키 목록](#단축키-목록)
11. [자주 묻는 질문(FAQ)](#자주-묻는-질문faq)
12. [문제 해결](#문제-해결)

---

## 개요

**CattitudeFlow**는 코넬 방식 노트와 티켓 관리를 결합한 데스크톱 앱입니다.

노트 본문은 Markdown 형식으로 작성할 수 있으며, Cue(큐)·Note(노트)·Summary(요약)의 3개 페인으로 구성되는 코넬 방식 포맷을 지원합니다.  
티켓에는 코넬 방식 **메모**를 여러 개 추가할 수 있어, 작업에 관한 상세한 기록을 남길 수 있습니다.  
v1.0.5에서는 화면의 텍스트를 그대로 앱에 가져오는 **OCR 캡처** 기능에 더해, 본문 위에 자유롭게 배치할 수 있는 **스티커 메모**, 화면 공유를 위한 **프레젠테이션 보기**, **Markdown 수식(KaTeX)** 등의 기능이 추가되었습니다.

---

## 화면 구성

### 헤더 내비게이션

앱 상단 헤더에 다음 내비게이션 버튼이 표시됩니다.

| 버튼 | 설명 |
|--------|------|
| 홈 | 노트·티켓·TODO 목록 및 검색 |
| 노트 | 코넬 방식 노트 작성·편집 |
| 티켓 | 작업·티켓 관리 |
| 포커스 | 중요도 점수에 따른 노트/티켓 목록 |
| OCR 메모 | OCR 캡처로 가져온 메모 목록 및 편집 |
| 설정 | 앱 설정 |

### InfoPill(정보 표시)

헤더 오른쪽에 다음 정보가 표시됩니다.

- **남은 일수**: 체험 기간의 남은 일수(예: 7일 남음)
- **여유 공간**: 디스크 여유 공간 상태(OK/적음)

### 창 조작 버튼

네이티브 앱으로 실행될 때 헤더 맨 오른쪽에 표시됩니다.

| 버튼 | 동작 |
|--------|------|
| `_` | 최소화 |
| `□` | 최대화/복원 |
| `×` | 닫기 |

### 제목 표시줄 빠른 버튼

| 버튼 | 동작 |
|--------|------|
| `✂` | 스니펫 관리 대화상자 열기(`Ctrl+I`) |
| `☀` | 스포트라이트 커서 ON/OFF |
| `☀ ▾` | 현재 스포트라이트 프리셋 전환 |
| `🖥` 프레젠테이션 보기 | 실시간 미리보기/미리보기의 가독성을 높이는 **프레젠테이션 보기** ON/OFF (화면 공유용) |

스니펫 버튼에는 저장된 개수 배지가 표시됩니다. 스포트라이트 버튼의 색상은 현재 선택된 프리셋 색상과 연동됩니다.

**프레젠테이션 보기** 는 실시간 미리보기와 미리보기의 글자 크기·줄 간격·대비를 강조하여 화면 공유 시에도 보기 쉽게 합니다. ON/OFF 상태는 앱에 기억되며 테마 배색이나 저장된 데이터에는 영향을 주지 않습니다.

### 시스템 트레이 메뉴

시스템 트레이 아이콘을 오른쪽 클릭하면 다음 메뉴가 표시됩니다.

| 항목 | 설명 |
|------|------|
| 표시 / 숨기기 | 메인 창 표시·숨기기 전환 |
| 스포트라이트 | 스포트라이트 커서 ON/OFF |
| 스니펫 | 스니펫 관리 대화상자 열기 |
| OCR | 화면 캡처 시작 |
| 종료 | 앱 종료 |

각 항목 오른쪽에는 현재 설정된 글로벌 핫키가 표시됩니다.  
핫키는 설정 → 단축키 에서 사용자 지정할 수 있습니다.

---

## 홈 화면

홈 화면은 3열 구성으로, 왼쪽 메뉴·가운데 리스트·오른쪽 Peek 미리보기로 구성됩니다.

### 왼쪽 패널(메뉴)

**필터 목록**

| 항목 | 설명 |
|------|------|
| 노트 | 노트만 표시 |
| 티켓 | 티켓만 표시 |
| TODO | 모든 노트의 TODO 항목 표시 |
| 이미지 | 첨부 이미지 목록 표시 |
| 모두 | 노트 + 티켓 + TODO를 함께 표시 |

각 항목에는 개수가 표시됩니다. 태그를 선택 중인 경우 `태그 개수 / 전체 개수` 형식으로 표시됩니다.

**태그 필터**

노트와 티켓의 태그가 통합되어 칩 형태로 표시됩니다.  
태그를 클릭하면 해당 태그를 가진 항목만 표시됩니다. `모두`를 클릭하면 필터가 해제됩니다.

### 가운데 패널(리스트)

**검색 바**

- 플레이스홀더: `전체 텍스트 검색(Ctrl+K) 예: has:url code:powershell foo`
- `Ctrl+K`로 포커스
- `지우기` 버튼으로 검색어 초기화
- `::`를 빠르게 입력하면 고정한 스니펫 모달을 열 수 있습니다

**필터 칩**

검색 바 아래에 필터 칩이 표시됩니다(모두 / 노트 / 티켓 / TODO).  
TODO 선택 시 **범위** 칩도 표시됩니다(모두 / 오늘).  
날짜 필터가 활성화된 경우 `date: YYYY-MM-DD ×` 칩이 표시되며, 클릭하여 해제할 수 있습니다.

**리스트 조작**

| 동작 | 작동 |
|------|------|
| 싱글 클릭 | 오른쪽 페인에 Peek 미리보기 표시 |
| 더블 클릭 | 해당 노트/티켓 화면으로 이동 |
| `Ctrl+Click` | 해당 화면으로 이동 |
| `Enter` | 해당 화면으로 이동 |

**티켓 메모 행**

티켓에 메모가 등록되어 있는 경우, 티켓 행 아래에 메모가 자식 행으로 표시됩니다.
메모가 있는 티켓에는 `▼ / ▶` 토글이 표시되어 메모 트리를 펼치거나 접을 수 있습니다.

| 동작 | 작동 |
|------|------|
| 싱글 클릭 | 티켓 화면으로 이동하여 메모 열기 |
| `⛶`(전체 화면 버튼) | 메모를 리스트 없는 전체 화면 미리보기로 표시 |

**이미지 탭 선택 시**

- 이미지 썸네일·경로·생성일·크기·참조 원본 개수가 표시됩니다
- 전용 검색 바(uid / relPath로 검색)가 표시됩니다
- 리스트 끝까지 스크롤하면 추가 로드가 가능합니다

### 오른쪽 패널(Peek 미리보기)

항목을 싱글 클릭하면 오른쪽 페인에 미리보기가 표시됩니다.

- 노트: 코넬 방식 미리보기(Cue / Note / Summary)
- 티켓: 티켓 정보 미리보기
- 이미지: 이미지 미리보기 및 참조 중인 노트/티켓 목록
- 티켓 메모: 메모의 코넬 방식 미리보기(리스트 패널 없음)
- **전체 화면 버튼** (⛶): 미리보기를 전체 화면으로 표시
- **축소/확대**: 코넬 방식 미리보기 크기 전환(1장 표시 / 읽기 쉬움)
- 외부 편집기에서 수정한 파일도 자동 감지되어 목록과 미리보기에 반영됩니다

### 홈 화면 키보드 단축키

| 단축키 | 동작 |
|----------------|------|
| `Ctrl+K` | 검색 바에 포커스 |
| `Ctrl+Shift+T` | 오늘 날짜 필터를 적용하고 이번 달 캘린더를 표시 |

---

## 노트 화면

### 화면 구성

노트 화면은 **왼쪽 리스트**와 **오른쪽 에디터**의 2열 구성입니다.

### 왼쪽 패널(노트 리스트)

**새로 만들기**

- `새로 만들기` 버튼 또는 `+`에서 새 노트 대화상자를 엽니다
- 대화상자에서 제목과 날짜(기본값: 오늘)를 입력하여 생성
- 같은 제목의 노트가 이미 있으면 기존 노트를 엽니다

**자식 노트 만들기**

- 노트를 선택한 상태에서 `＋ 자식 노트` 버튼을 클릭하면 선택한 노트의 자식 노트를 만듭니다
- 자식 노트는 리스트에서 들여쓰기된 행으로 표시됩니다
- 현재 열려 있는 노트가 이미 자식 노트인 경우, 같은 부모를 가진 형제 노트로 생성됩니다(손자 노트는 생성되지 않음)

**검색**

- `/` 키로 검색 바에 포커스
- Host의 Full-text search를 통한 노트 내용 전체 텍스트 검색 가능

**태그 필터**

태그 칩을 클릭하여 태그로 노트를 필터링할 수 있습니다.

**고정**

- 노트 행의 ★ 아이콘으로 고정 토글
- 고정된 노트는 리스트 최상단에 표시
- `고정됨`과 `모두` 섹션으로 나뉘어 표시
- 고정 상태는 Workspace에 영구 저장됩니다(최대 100개)

### 오른쪽 패널(에디터)

**메타 정보 필드**

| 필드 | 설명 |
|------------|------|
| 제목 | 노트 제목(필수) |
| 날짜 | YYYY-MM-DD 형식(예: 2026-04-26) |
| 태그 | 쉼표 구분(예: ops, daily) |

**코넬 방식 에디터**

노트 본문은 다음 3개 페인으로 구성됩니다.

| 페인 | 용도 |
|--------|------|
| **Cue**(큐) | 질문·키워드·힌트 |
| **Note**(노트) | 주요 내용·상세 메모 |
| **Summary**(요약) | 정리·결론 |

**페인 표시 모드**

페인 모드 토글로 표시 모드를 전환할 수 있습니다.

| 모드 | 표시 |
|--------|------|
| `1` | 1페인(활성 페인만 표시) |
| `2` | 2페인 |
| `3` | 3페인(모든 페인을 동시에 표시) ※ 기본값 |

전환한 모드는 즉시 반영되며, 노트를 다시 열었을 때도 이전 표시 모드가 복원됩니다.

**탭 전환**

| 탭 | 설명 |
|------|------|
| 편집 | CodeMirror 에디터 |
| 미리보기 | Markdown 미리보기(목차 포함) |

- 편집 중 `Ctrl+F`를 누르면 앱 언어에 맞는 검색 패널을 열 수 있습니다
- 미리보기 탭에서는 현재 배경색을 반영한 PDF 내보내기를 사용할 수 있습니다

**Markdown 툴바**

편집 탭 상단의 툴바에서 각종 서식을 삽입할 수 있습니다.

| 버튼 | 서식 |
|--------|------|
| 굵게 | `**텍스트**` |
| 기울임꼴 | `*텍스트*` |
| 취소선 | `~~텍스트~~` |
| 밑줄 | `<u>텍스트</u>` |
| 글자색 | `<span style="color:#XXXXXX">텍스트</span>`(20색) |
| 글머리 기호 목록 | `- 텍스트` |
| 번호 매기기 목록 | `1. 텍스트` |
| TODO 목록 | `- [ ] 텍스트` |
| 테이블 | 테이블 템플릿 삽입 |
| 알림 박스 | GFM 알림(NOTE / TIP / IMPORTANT / WARNING / CAUTION) |
| 제목 | H1 / H2 / H3 |

**글자색 팔레트(20색)**

Red / Crimson / Orange / Amber / Yellow / Lime / Green / Teal / Cyan / Sky /  
Blue / Indigo / Purple / Violet / Pink / Rose / Brown / Slate / Gray / Black

**이미지 붙여넣기**

클립보드의 이미지를 에디터에 붙여넣으면 자동으로 Workspace에 저장되고 Markdown 이미지 문법으로 삽입됩니다.  
가져오는 동안에는 "이미지를 가져오는 중…" 오버레이가 표시됩니다.

**스티커 메모**

노트 본문 위에 자유롭게 배치할 수 있는 스티커 메모를 붙일 수 있습니다. 에디터 본문에서 마우스 오른쪽 버튼을 클릭하고 컨텍스트 메뉴의 `스티커 메모 추가` 를 선택합니다.

- 추가한 스티커 메모에는 자동으로 포커스가 이동하여 바로 입력할 수 있습니다(입력 중 포커스를 빼앗기지 않습니다)
- 상단 바를 드래그하여 이동합니다. 스티커 메모는 본문 위치에 고정되어 스크롤과 편집을 따라갑니다
- 왼쪽 위 색상 버튼으로 색을 전환합니다(노랑 → 분홍 → 파랑 → 초록 순환)
- 텍스트 영역의 오른쪽 아래를 드래그하면 크기를 변경할 수 있으며, 변경한 크기는 저장됩니다
- `✕` 버튼으로 삭제합니다(확인 대화상자 있음)
- `Ctrl+;` 로 날짜, `Ctrl+Shift+;` 로 시간을 삽입할 수 있습니다
- 스티커 메모는 노트 본문에 저장되므로 Zip 백업에도 포함됩니다

**수식(Math)**

Markdown 미리보기와 실시간 미리보기는 KaTeX 를 통한 수식 표시를 지원합니다.

- 인라인 수식: `$E = mc^2$`
- 블록 수식: 독립된 줄에 `$$` 로 감싸서 작성합니다

```
$$
\int_0^1 x^2 dx = \frac{1}{3}
$$
```

**저장 상태**

오른쪽 상단에 저장 상태가 표시됩니다.

| 상태 | 표시 |
|------|------|
| 저장됨 | `저장됨` |
| 저장 안 됨 | `저장 안 됨` |
| 저장 중 | `저장 중…` |

- 에디터 본문과 미리보기 본문의 글꼴은 설정 → 에디터 에서 변경할 수 있습니다
- 외부 편집기에서 노트 파일을 수정한 경우에도 자동으로 다시 불러옵니다

**삭제**

휴지통 아이콘으로 노트를 삭제할 수 있습니다. 고정 중이거나 저장되지 않은 경우 경고가 표시됩니다.  
자식 노트가 존재하는 경우, 자식 노트도 함께 삭제할지 확인 대화상자가 표시됩니다.

### 노트 화면 키보드 단축키

| 단축키 | 동작 |
|----------------|------|
| `Ctrl+Enter` 또는 `Ctrl+S` | 저장 |
| `Ctrl+1` | Cue 페인으로 이동 |
| `Ctrl+2` | Note 페인으로 이동 |
| `Ctrl+3` | Summary 페인으로 이동 |
| `Ctrl+E` | 편집 탭으로 전환 |
| `Ctrl+P` | 미리보기 탭으로 전환 |
| `/` | 노트 검색 바에 포커스 |

---

## 티켓 화면

티켓은 작업이나 ToDo를 관리하기 위한 기능입니다. 코넬 방식 본문·상태 관리·댓글·메모를 지원합니다.

### 왼쪽 패널(티켓 리스트)

**검색·필터링**

| 동작 | 설명 |
|------|------|
| `/` 키 | 검색 바에 포커스 |
| 우선순위 필터 | 모두 / P0 / P1 / P2 / P3 |
| 정렬 | 업데이트일 / 기한 / 우선순위 |

**새로 만들기**

- `새로 만들기` 버튼으로 제목 입력 폼이 표시됩니다
- 제목을 입력하고 `Enter`로 생성

**메모 리스트**

티켓을 선택하면 해당 티켓에 등록된 메모가 아래쪽에 리스트로 표시됩니다.

| 동작 | 설명 |
|------|------|
| 메모 행 클릭 | 해당 메모를 오른쪽 패널에서 열기 |
| `👁` 아이콘 | 메모를 리스트에서 숨기기/다시 표시 |
| `🗑` 아이콘 | 메모 삭제(확인 대화상자 표시) |

### 오른쪽 패널(티켓 에디터)

티켓을 선택하면 오른쪽 패널에 에디터가 표시됩니다.

**메타 정보 필드**

| 필드 | 설명 |
|------------|------|
| 제목 | 티켓 제목(필수) |
| 상태 | 사용자 지정 열(설정 화면에서 변경 가능. 예: backlog / doing / done) |
| 기한 | YYYY-MM-DD 형식 |
| 우선순위 | P0(최고) / P1 / P2 / P3(최저) |
| 라벨 | 쉼표 구분(예: urgent, ops) |

**탭 전환**

| 탭 | 설명 |
|------|------|
| 편집 | 코넬 방식 에디터(Cue / Note / Summary) |
| 미리보기 | Markdown 미리보기(코넬 방식 3페인) |
| 댓글(n) | 댓글 추가·열람 |
| 메모 | 선택한 티켓의 메모 목록과 편집 |

**삭제**

휴지통 아이콘으로 티켓을 삭제합니다(확인 대화상자 표시).  
메모가 등록되어 있는 경우, 메모 개수를 포함한 확인 메시지가 표시됩니다.

### 메모 기능

티켓에는 여러 개의 코넬 방식 **메모**를 추가할 수 있습니다. 회의 메모, 조사 기록, 보충 자료를 티켓 본문과 분리하여 정리할 수 있습니다.

**메모 추가**

- `메모` 탭의 `＋ 메모 추가` 버튼이나 왼쪽 패널 하단의 같은 버튼으로 즉시 생성할 수 있습니다
- 새 메모의 기본 제목은 `메모`입니다

**메모 에디터**

메모 에디터는 노트와 같은 코넬 방식 레이아웃을 사용하며 1 / 2 / 3페인 표시를 전환할 수 있습니다.

| 탭 | 설명 |
|------|------|
| 편집 | 코넬 방식 에디터(Cue / Note / Summary) |
| 미리보기 | Markdown 미리보기(코넬 방식 1 / 2 / 3페인 표시) |

- 제목을 입력하면 즉시 자동 저장이 시작됩니다
- 페인 모드 변경은 바로 반영되며 다음에 열 때도 유지됩니다
- 미리보기 탭에서 PDF 내보내기를 사용할 수 있습니다
- 노트와 마찬가지로 Note 창 본문에서 마우스 오른쪽 버튼 클릭 → `스티커 메모 추가` 로 스티커 메모를 붙일 수 있습니다

**메모 정렬**

메모 리스트 각 행에 표시되는 `▲` / `▼` 버튼으로 메모 순서를 변경할 수 있습니다.

**메모 조작**

| 동작 | 설명 |
|------|------|
| 메모 행 클릭 | 메모 에디터 열기 |
| `👁` 아이콘 | 메모를 리스트에서 숨기기/다시 표시(데이터는 유지) |
| `🗑` 아이콘 | 메모 삭제(확인 대화상자 표시) |
| `⛶`(전체 화면) | 리스트 패널 없이 전체 화면 표시로 메모 열기 |
| `▲` / `▼` | 메모 표시 순서 변경 |

> 메모는 티켓에 연결되어 있으며, 티켓을 삭제하면 메모도 함께 삭제됩니다.

### 티켓 화면 키보드 단축키

| 단축키 | 동작 |
|----------------|------|
| `Ctrl+Enter` | 저장 |
| `/` | 검색 바에 포커스 |

---

## 포커스 화면

중요한 노트와 티켓을 점수순으로 표시하여 집중적으로 검토·열람하기 위한 화면입니다.

### 표시 모드

| 모드 | 설명 |
|--------|------|
| 노트 | 노트를 점수순으로 표시 |
| 티켓 | 티켓을 점수순으로 표시 |

### 점수 계산

노트 점수는 다음 요소로 계산됩니다.

- **편집 횟수**(지난 30일): 설정에서 ON/OFF 전환 가능
- **참조 수(incomingLinks)**: 다른 노트에서 참조된 횟수

### 화면 구성

| 영역 | 내용 |
|------|------|
| 왼쪽 리스트 | 순위·제목·참조 수·업데이트일 |
| 오른쪽 미리보기 | 선택한 노트의 코넬 방식 미리보기 |
| `최대` | 목록에 표시할 최대 개수(5~200) |

### 조작

| 동작 | 작동 |
|------|------|
| 클릭 | 노트를 선택하고 미리보기 표시 |
| 더블 클릭 / `Enter` | 노트/티켓 화면에서 열기 |
| `전체 화면 보기` 버튼 | 전체 화면 뷰어 열기 |

### 전체 화면 뷰어

- 왼쪽 리스트에서 항목 전환 가능
- 검색 바로 제목 검색 가능
- `열기` 버튼으로 해당 화면으로 이동
- `ESC` 또는 `닫기`로 종료

### 스니펫 검색과 고정

- 검색 바가 비어 있으면 Focus 모드로 유지되며, 편집 활동 점수 순서로 노트와 티켓을 표시합니다
- 키워드를 입력하면 Snippet 모드로 전환되고, 일치하는 코넬 방식 페인만 강조 표시됩니다
- `::`를 빠르게 입력하면 고정된 스니펫 모달을 열어 바로 복사할 수 있습니다
- 제목 표시줄의 `✂` 버튼이나 `Ctrl+I`로 언제든지 스니펫 관리 대화상자를 열 수 있습니다

### 포커스 화면 키보드 단축키

| 단축키 | 동작 |
|----------------|------|
| `Enter` | 선택한 항목 열기 |
| `ESC` | 전체 화면 뷰어 닫기 |

---

## OCR 메모 화면

화면 위의 임의 영역을 캡처해 텍스트를 인식하고, **OCR 메모**로 자동 저장·열람·편집할 수 있습니다.  
인식 언어는 일본어, 영어, 중국어, 한국어, 프랑스어, 독일어를 지원합니다.

### OCR 캡처 시작

다음 중 하나의 방법으로 캡처를 시작합니다.

| 방법 | 동작 |
|------|------|
| 글로벌 핫키 | `Ctrl+Shift+F12`(기본값)를 누르기 |
| 시스템 트레이 | 트레이 아이콘을 오른쪽 클릭 → **OCR** 선택 |

캡처가 시작되면 화면이 반투명해집니다. 마우스로 드래그하여 인식할 사각형 영역을 선택하면 OCR 처리가 실행됩니다.

### OCR 메모 목록

헤더의 **OCR 메모**를 클릭하면 목록 화면이 표시됩니다.

| 열 | 내용 |
|----|------|
| 캡처 일시 | OCR을 실행한 날짜와 시간 |
| 인식 텍스트 | OCR 결과의 앞부분 |

### OCR 메모 편집 화면

목록에서 메모를 선택하면 편집 화면이 표시됩니다.

**이미지 영역(설정에서 "이미지 저장"이 ON인 경우)**

캡처한 화면의 잘라낸 이미지가 코르크 배경에 고정된 카드 형태로 표시됩니다.

**텍스트 영역**

OCR로 인식된 텍스트가 Markdown 에디터에 표시됩니다.  
내용은 자유롭게 편집·수정할 수 있습니다.

| 동작 | 설명 |
|------|------|
| 텍스트 편집 | 에디터에서 직접 수정 |
| 복사 | 텍스트를 클립보드에 복사 |
| 삭제 | 선택한 메모 삭제(확인 대화상자 표시) |

### 일괄 삭제

리스트 상단의 **모두 삭제** 버튼을 클릭하면 확인 대화상자가 표시됩니다.  
확인 후 모든 OCR 메모가 삭제됩니다.

> ⚠️ **이 작업은 되돌릴 수 없습니다.** 필요한 텍스트는 미리 복사해 두세요.

---

## 설정 화면

헤더의 **설정**에서 각종 설정을 변경할 수 있습니다. 왼쪽 사이드바에서 카테고리를 선택합니다.

### 일반(General)

| 설정 | 설명 |
|------|------|
| 언어 | UI 표시 언어(system / ja / en / zh / ko / fr / de) |
| 로케일 | 숫자·날짜 형식의 로케일(비워 두면 OS 설정 사용, 예: ja-JP) |
| 테마 프리셋 | 색상 테마 |

- `미리보기 적용`: 선택한 테마를 즉시 미리보기
- `테마 저장`: 테마를 Host에 저장
- `언어/로케일 저장`: 언어 설정 저장
- 저장한 언어는 파일 대화상자, 시작 오류, `Ctrl+F` 검색 패널 같은 네이티브/내장 UI에도 함께 적용됩니다

### 작업공간(Workspace)

| 설정 | 설명 |
|------|------|
| WorkspaceRoot | 노트와 티켓의 저장 경로 |

- `다시 불러오기`: 현재 Workspace 설정을 다시 가져오기
- `저장`: 변경한 경로 저장

### 노트

| 설정 | 설명 |
|------|------|
| 자동 저장 | 입력 시 자동 저장 여부(ON/OFF) |
| 자동 저장 간격(ms) | 자동 저장 간격(200~10,000ms, UI 로컬 설정) |

### 에디터

| 설정 | 설명 |
|------|------|
| 글꼴 패밀리 | 에디터 본문과 미리보기 본문에 사용할 글꼴 |
| 글꼴 크기(px) | 본문 글꼴 크기 |
| 미리보기 배경 렌더링 | ON이면 카드와 코드 블록 등의 배경색을 렌더링합니다. OFF이면 배경색이 투명해지며 PDF 출력에도 적용됩니다.(기본: OFF) |
| 제목·표에 테마 색 사용 | ON이면 제목과 표 머리글에 테마 강조색(기본색)을 반영합니다. OFF(기본)이면 본문 색으로 통일되어 PDF 출력과 동일한 차분한 배색이 됩니다. |

- 입력 중인 값도 저장 전에 즉시 미리보기 됩니다
- `기본값으로 되돌리기`로 글꼴, 미리보기 배경 렌더링 OFF, 테마 색 OFF 등 기본값을 복원할 수 있습니다

### 티켓

| 설정 | 설명 |
|------|------|
| 자동 저장 | 티켓 편집 시 자동 저장 여부(ON/OFF) |
| 자동 저장 간격(ms) | 자동 저장 간격(UI 로컬 설정) |
| 열(Columns) | 티켓의 Status 후보(쉼표 구분)<br>예: `backlog, doing, done, blocked, archived` |
| 티켓 ID 표시 | 목록/미리보기 등에 티켓 ID 표시(UI 로컬 설정) |

### 포커스

| 설정 | 설명 |
|------|------|
| 최대 개수 | 포커스 화면 목록의 최대 개수 |
| 편집 횟수를 순위에 사용 | note.save 횟수를 Focus 점수에 가산 |
| 활동 로그(Activity) | 편집 횟수 집계를 활성화(본문·검색 쿼리는 저장되지 않음) |

> ※ autoSave에 의한 연속 저장은 "1분에 1회"로 반올림하여 계산됩니다.

### 스포트라이트

| 설정 | 설명 |
|------|------|
| 프리셋 1~5 | 5개의 고정 프리셋 중 사용할 스포트라이트 선택 |
| 이름 | 각 프리셋의 표시 이름(최대 20자) |
| 크기(px) | 스포트라이트 원의 지름(20~200px) |
| 색상 | 스포트라이트 원과 헤더 아이콘에 사용하는 색상 |
| 투명도 | 스포트라이트의 투명도 수준 |

- 제목 표시줄의 `☀` 버튼으로 ON/OFF를 전환할 수 있습니다
- `↩ 기본값으로 되돌리기`로 색상과 크기를 기본값으로 되돌릴 수 있습니다

### 단축키

글로벌 핫키(WPF 레벨)를 변경할 수 있습니다.

| 단축키 | 기본값 | 설명 |
|----------------|-----------|------|
| 표시 / 숨기기 | `Ctrl+Shift+F9` | 메인 창 표시·숨기기 전환 |
| 스포트라이트 | `Ctrl+Shift+F10` | 스포트라이트 커서 ON/OFF |
| 스니펫 | `Ctrl+Shift+F11` | 스니펫 관리 대화상자 열기 |
| OCR 메모 | `Ctrl+Shift+F12` | OCR 캡처 시작 |

**변경 방법**

1. 변경하려는 단축키 입력 칸을 클릭합니다("클릭하여 변경" 표시)
2. 새로운 키 조합을 누릅니다(수정 키 필요)
3. `저장` 버튼으로 확정합니다
4. `재설정` 버튼으로 기본값으로 되돌립니다

설정을 저장하면 시스템 트레이 메뉴의 핫키 표시에도 즉시 반영됩니다.

### OCR

| 설정 | 설명 |
|------|------|
| 이미지 저장 | OCR 캡처 시 이미지를 메모에 첨부(기본값: ON) |

- **ON**: OCR 결과 텍스트와 함께 캡처 이미지를 저장합니다. OCR 메모 편집 화면에 코르크보드 스타일의 이미지 영역이 표시됩니다.
- **OFF**: 텍스트만 저장합니다.

### 유지 관리

| 동작 | 설명 |
|------|------|
| **Zip 내보내기** | Workspace(노트, 티켓, OCR 메모, 이미지)를 Zip 파일로 백업 |
| **Zip에서 복원** | Zip에서 노트, 티켓, OCR 메모를 복원(기존 데이터는 대체됩니다) |
| **로그 폴더 열기** | 앱 로그 폴더를 파일 관리자에서 열기 |
| **로그 Zip 출력** | 로그를 Zip 형식으로 내보내기 |
| **라이선스 폴더 열기** | 포함된 라이선스 파일 폴더 열기 |

> ⚠️ **복원 작업은 기존 노트와 티켓을 완전히 대체합니다.** 반드시 백업을 만든 후 실행하세요.

---

## 검색 구문 레퍼런스

홈 화면의 전체 텍스트 검색 바에서는 다음 특수 구문을 사용할 수 있습니다.

| 구문 | 설명 | 예 |
|------|------|----|
| 일반 키워드 | 공백 구분 AND 검색 | `메모 회의` |
| `"구문"` | 구문 검색(여러 단어를 하나의 묶음으로 검색) | `"오늘 작업"` |
| `has:url` | URL을 포함한 노트/티켓 검색 | `has:url` |
| `code:any` | 코드 블록을 포함한 항목 검색 | `code:any` |
| `code:<언어>` | 지정한 언어의 코드 블록을 포함한 항목 검색 | `code:powershell` |

**예:**
```
has:url code:powershell 메모
```
→ URL을 포함하고, PowerShell 코드 블록을 포함하며, "메모"라는 키워드를 포함한 항목

※ `::`를 빠르게 입력하면 검색 결과 대신 고정된 스니펫 모달을 열 수 있습니다.

---

## 단축키 목록

### 글로벌 핫키(WPF 레벨·사용자 지정 가능)

설정 → 단축키 에서 변경할 수 있습니다.

| 단축키(기본값) | 동작 |
|------------------------------|------|
| `Ctrl+Shift+F9` | 메인 창 표시 / 숨기기 |
| `Ctrl+Shift+F10` | 스포트라이트 커서를 ON/OFF |
| `Ctrl+Shift+F11` | 스니펫 관리 대화상자 열기 |
| `Ctrl+Shift+F12` | OCR 캡처 시작 |

### 앱 내 공통

| 단축키 | 동작 |
|----------------|------|
| `Ctrl+K` | 홈 검색 바에 포커스 |
| `Ctrl+I` | 스니펫 관리 대화상자 열기 |
| `::` | 고정된 스니펫 모달 열기 |
| `Ctrl+Shift+T` | 오늘 날짜 필터 적용 |
| `Ctrl+마우스 휠` | 화면 확대/축소 |
| `Ctrl+0` | 확대/축소를 100%로 재설정 |

### 노트 화면

| 단축키 | 동작 |
|----------------|------|
| `Ctrl+Enter` / `Ctrl+S` | 저장 |
| `Ctrl+1` | Cue 페인으로 이동 |
| `Ctrl+2` | Note 페인으로 이동 |
| `Ctrl+3` | Summary 페인으로 이동 |
| `Ctrl+E` | 편집 탭으로 전환 |
| `Ctrl+P` | 미리보기 탭으로 전환 |
| `/` | 검색 바에 포커스 |
| `Ctrl+F` | 에디터 내 검색 패널 열기 |

### 티켓 화면

| 단축키 | 동작 |
|----------------|------|
| `Ctrl+Enter` | 저장 |
| `/` | 검색 바에 포커스 |
| `Ctrl+F` | 에디터 내 검색 패널 열기 |

### 포커스 화면

| 단축키 | 동작 |
|----------------|------|
| `Enter` | 선택한 항목 열기 |
| `ESC` | 전체 화면 뷰어 닫기 |

### 홈 화면(리스트 조작)

| 단축키 | 동작 |
|----------------|------|
| `Enter` | 선택한 항목으로 이동 |
| `Ctrl+Click` | 선택한 항목으로 이동 |

---

## 자주 묻는 질문(FAQ)

### Q. 포커스 점수는 어떻게 결정되나요?
**A.** 다음 요소로 계산됩니다.
- **편집 횟수(edit30d)**: 지난 30일간의 저장 횟수(연속 자동 저장은 1분에 1회로 반올림)
- **참조 수(incomingLinks)**: 다른 노트에서 링크된 횟수

설정 → 포커스 → **편집 횟수를 순위에 사용**에서 ON/OFF를 전환할 수 있습니다.

---

### Q. 티켓에 메모를 몇 개든 추가할 수 있나요?
**A.** 개수 제한은 없습니다. `＋ 메모 추가` 버튼으로 추가할 수 있습니다. 불필요해진 메모는 휴지통 아이콘으로 삭제하고, 일시적으로 숨기고 싶다면 👁 아이콘으로 비표시할 수 있습니다.

---

### Q. TODO는 어떻게 삭제하나요?
**A.** TODO는 노트 안의 `- [ ] 텍스트` 형식 행입니다. 노트를 편집할 때 해당 행을 삭제하거나, `- [x] 텍스트`처럼 완료 처리하세요. 홈 화면의 TODO 리스트는 자동으로 업데이트됩니다.

---

### Q. 티켓 상태를 추가하거나 변경하고 싶습니다
**A.** 설정 → 티켓 → **열(Columns)** 에 쉼표로 구분한 상태 이름을 입력한 뒤 저장하세요.  
예: `backlog, doing, done, blocked, archived`

---

### Q. 여러 기기에서 동기화할 수 있나요?
**A.** WorkspaceRoot에 공유 폴더(NAS 또는 클라우드 동기화 폴더)를 지정하여 참조할 수는 있지만, **동시 편집은 데이터 충돌 가능성이 있으므로 권장되지 않습니다**.

---

### Q. 자식 노트가 다시 자식 노트를 가질 수 있나요?
**A.** 자식 노트의 중첩은 1단계로 제한됩니다. 자식 노트를 선택한 상태에서 `＋ 자식 노트`를 누르면, 같은 부모를 가진 형제 노트로 생성됩니다(손자 노트는 생성되지 않음).

---

### Q. OCR이 인식할 수 있는 언어는 무엇인가요?
**A.** 일본어, 영어, 중국어, 한국어, 프랑스어, 독일어를 지원합니다. 화면 안의 텍스트에 여러 언어가 섞여 있어도 인식할 수 있지만, 정확도는 언어와 글자 크기에 따라 달라질 수 있습니다.

---

### Q. OCR 캡처 이미지를 저장하고 싶지 않습니다
**A.** 설정 → OCR → **이미지 저장** 을 OFF로 바꾸면 텍스트만 저장됩니다. 기존 메모에 첨부된 이미지에는 영향을 주지 않습니다.

---

### Q. 글로벌 핫키를 변경하고 싶습니다
**A.** 설정 → 단축키 에서 4개의 핫키를 각각 변경할 수 있습니다. 입력 칸을 클릭한 뒤 새로운 키 조합을 누르고 `저장`을 클릭하세요.

---

### Q. 자동 저장과 고정을 동시에 사용할 수 있나요?
**A.** 네. 자동 저장은 노트 본문과 메타 정보를 주기적으로 저장하고, 고정 상태는 별도의 메커니즘으로 저장됩니다.

---

## 문제 해결

### 앱이 시작되지 않음
1. **WebView2 Runtime** 이 설치되어 있는지 확인
2. Host 측(백엔드 프로세스)이 정상적으로 시작되었는지 확인
3. 설정 → 유지 관리 → **로그 폴더 열기**에서 오류 로그 확인

### 데이터를 불러올 수 없거나 오류가 표시됨

| 오류 | 대처 |
|--------|------|
| `E_WS_NOT_WRITABLE` | Workspace 폴더의 쓰기 권한 확인 |
| `E_WS_INVALID_ROOT` | 설정 → 작업공간(Workspace) 에서 경로 확인 및 수정 |
| `E_APP_NOT_READY` | 시작 완료를 기다린 후 다시 시도 |
| `E_WEBVIEW2_UNAVAILABLE` | WebView2 Runtime 설치 |
| `E_SETTINGS_CORRUPT` | 설정이 자동으로 초기화됩니다. 다시 설정해 주세요 |

파일 읽기/쓰기 오류와 네이티브 대화상자 메시지는 설정한 표시 언어로 표시됩니다.

### 자동 저장이 동작하지 않음
1. 설정 → 노트/티켓 → **자동 저장** 이 활성화되어 있는지 확인
2. autoSaveMs 가 `200~10,000` 범위 내인지 확인
3. 노트/티켓 내용이 실제로 변경되었는지 확인(변경이 없으면 저장되지 않음)

### 검색 결과가 표시되지 않음
1. 검색어 철자 확인
2. 태그 필터나 날짜 필터가 적용되어 있지 않은지 확인(필터 칩의 `×` 로 해제)
3. 필터를 **모두** 로 전환한 뒤 다시 검색

### Peek 미리보기가 표시되지 않음
- 더블 클릭이 아니라 **싱글 클릭** 으로 항목을 클릭
- 창 너비가 좁다면 넓혀서 오른쪽 페인이 보이게 함

### OCR이 동작하지 않음
1. OCR 엔진 초기화에 몇 초가 걸릴 수 있습니다. 잠시 기다린 후 다시 시도해 주세요
2. 앱을 다시 시작한 후 다시 시도해 주세요
3. 설정 → 유지 관리 → **로그 폴더 열기**에서 오류 로그를 확인해 주세요
